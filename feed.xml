<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jefferyy-peng.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jefferyy-peng.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-13T04:57:04+00:00</updated><id>https://jefferyy-peng.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Mechanistic Understanding of Hallucination in Multimodal Models</title><link href="https://jefferyy-peng.github.io/blog/2026/Mechanistic-Understanding-of-Hallucination-in-Multimodal-Models/" rel="alternate" type="text/html" title="Mechanistic Understanding of Hallucination in Multimodal Models"/><published>2026-05-02T00:00:00+00:00</published><updated>2026-05-02T00:00:00+00:00</updated><id>https://jefferyy-peng.github.io/blog/2026/Mechanistic-Understanding-of-Hallucination-in-Multimodal-Models</id><content type="html" xml:base="https://jefferyy-peng.github.io/blog/2026/Mechanistic-Understanding-of-Hallucination-in-Multimodal-Models/"><![CDATA[<p><strong>TL;DR:</strong></p> <h1 id="research-idea-1-mechanistic-understanding-of-hallucination-in-multimodal-models">Research Idea 1: Mechanistic Understanding of Hallucination in Multimodal Models</h1> <h2 id="working-title">Working title</h2> <p><strong>When Do Multimodal Models Hallucinate? Mechanistic Statistics for Separating Grounded and Hallucinated Responses</strong></p> <h2 id="core-research-question">Core research question</h2> <p>Multimodal large language models (MLLMs / LVLMs) often hallucinate: they mention objects, attributes, relations, or reasoning conclusions that are not supported by the image. The goal of this project is not only to detect hallucination from outputs, but to understand <strong>what changes inside the model</strong> when hallucination happens.</p> <p>The central question is:</p> <blockquote> <p>Can internal model statistics separate hallucinated and non-hallucinated generations before or during decoding?</p> </blockquote> <p>Examples of candidate statistics:</p> <ul> <li>Attention pattern between text tokens and image tokens</li> <li>Vision-aware attention head behavior</li> <li>Token-level attribution to image vs. language context</li> <li>Circuit attribution / causal edge attribution</li> <li>Representation geometry of grounded vs. hallucinated tokens</li> <li>Logit contribution from visual tokens vs. language prior</li> <li>Layer-wise shift from visual evidence to textual prior</li> </ul> <h2 id="motivation">Motivation</h2> <p>Current hallucination work in MLLMs focuses heavily on:</p> <ol> <li><strong>Benchmarks</strong>: measuring whether a model hallucinates.</li> <li><strong>Detection</strong>: classifying an output as hallucinated or not.</li> <li><strong>Mitigation</strong>: decoding or training methods that reduce hallucination.</li> </ol> <p>But for a research paper, a stronger mechanistic question is:</p> <blockquote> <p>When the model hallucinates, does it fail because it cannot see the visual evidence, or because later language-model components override the visual evidence?</p> </blockquote> <p>This is important because two visually similar failures may have different causes:</p> <ul> <li><strong>Perception failure</strong>: the visual encoder or cross-modal projection does not represent the object.</li> <li><strong>Grounding failure</strong>: the model sees the object but does not use the visual signal.</li> <li><strong>Language-prior failure</strong>: the decoder follows a likely textual pattern instead of the image.</li> <li><strong>Reasoning failure</strong>: the model grounds local objects correctly but draws an unsupported conclusion.</li> <li><strong>Instruction/prompt-induced failure</strong>: the prompt biases the model toward a wrong answer.</li> </ul> <h2 id="related-work-landscape">Related work landscape</h2> <h3 id="hallucination-benchmarks">Hallucination benchmarks</h3> <p>Useful benchmarks for labeling hallucinated vs. non-hallucinated outputs:</p> <ul> <li><strong>CHAIR</strong>: classic object hallucination metric for image captioning.</li> <li><strong>POPE</strong>: evaluates object hallucination using yes/no probing.</li> <li><strong>AMBER</strong>: evaluates hallucination across existence, attribute, and relation hallucination.</li> <li><strong>MMHal-Bench</strong>: evaluates open-ended hallucination in multimodal responses.</li> <li><strong>HallusionBench</strong>: tests visual illusion, knowledge conflict, and reasoning-related hallucination.</li> <li><strong>MME-Hall / MME variants</strong>: commonly used for MLLM hallucination evaluation.</li> </ul> <p>AMBER is useful because it explicitly separates hallucination types such as existence, attribute, and relation hallucination. POPE and CHAIR are useful for object hallucination but narrower.</p> <h3 id="detection-and-mitigation-work">Detection and mitigation work</h3> <p>Existing methods often use external evaluators, uncertainty, contrastive decoding, or attention-based signals. Examples include:</p> <ul> <li><strong>VADE</strong>: uses visual grounding information encoded in transformer attention maps for hallucination detection.</li> <li><strong>DAMRO</strong>: analyzes attention relationships between visual encoder and LLM decoder and uses this for hallucination mitigation.</li> <li><strong>VCD / LCD / ICD / CICD / SECOND-style methods</strong>: reduce hallucination by contrasting visual or language-prior distributions during decoding.</li> <li><strong>Internal representation / attention divergence methods</strong>: use internal attention or hidden-state differences as hallucination indicators.</li> </ul> <p>These papers suggest that internal signals are informative, but many of them are designed mainly for detection or mitigation rather than a fine-grained mechanistic explanation.</p> <h3 id="mechanistic-analysis-work">Mechanistic analysis work</h3> <p>Some recent work is closer to the proposed direction:</p> <ul> <li>Studies on prompt-induced hallucination identify specific attention heads whose ablation can reduce hallucination.</li> <li>Vision-aware head divergence metrics attempt to quantify whether attention heads are sensitive to visual context.</li> <li>Causal mediation analysis has been used to test which modules or representations contribute to hallucination.</li> <li>Some work argues that LVLM hallucination is often caused by language-prior dominance rather than total visual blindness.</li> </ul> <p>This makes the proposed project plausible: hallucination may have identifiable internal signatures.</p> <h2 id="proposed-hypothesis">Proposed hypothesis</h2> <p>The main hypothesis:</p> <blockquote> <p>Hallucinated tokens have lower causal dependence on image representations and higher dependence on language-prior circuits than grounded tokens.</p> </blockquote> <p>More specific hypotheses:</p> <ol> <li> <p><strong>Attention hypothesis</strong><br/> Hallucinated object/attribute tokens attend less to relevant image patches or attend more diffusely than grounded tokens.</p> </li> <li> <p><strong>Attribution hypothesis</strong><br/> For hallucinated tokens, the logit attribution from image tokens or vision-aware heads is lower, while attribution from previous text tokens is higher.</p> </li> <li> <p><strong>Circuit hypothesis</strong><br/> Grounded generations rely on circuits connecting image tokens to answer tokens; hallucinated generations rely more on text-only or prompt-copying circuits.</p> </li> <li> <p><strong>Representation hypothesis</strong><br/> Grounded and hallucinated token states become separable in hidden-state space at middle-to-late decoder layers.</p> </li> <li> <p><strong>Language-prior override hypothesis</strong><br/> The model may encode correct visual information early but lose or override it during decoding.</p> </li> </ol> <h2 id="possible-experimental-setup">Possible experimental setup</h2> <h3 id="step-1-build-a-hallucination-labeled-dataset">Step 1: Build a hallucination-labeled dataset</h3> <p>Use existing benchmarks to get examples labeled as hallucinated / non-hallucinated.</p> <p>Possible sources:</p> <ul> <li>POPE for object existence hallucination</li> <li>AMBER for existence / attribute / relation hallucination</li> <li>HallusionBench for reasoning and illusion-type hallucination</li> <li>MMHal-Bench for open-ended multimodal hallucination</li> </ul> <p>For each example, collect:</p> <ul> <li>Image</li> <li>Prompt</li> <li>Model response</li> <li>Hallucination label</li> <li>Hallucination type</li> <li>Hallucinated span if available</li> <li>Correct visual evidence if available</li> </ul> <h3 id="step-2-run-target-mllms">Step 2: Run target MLLMs</h3> <p>Candidate models:</p> <ul> <li>LLaVA-style models</li> <li>Qwen-VL / Qwen2.5-VL / Qwen3-VL style models</li> <li>InstructBLIP / MiniGPT-style models</li> <li>Open-source models with accessible activations and attention maps</li> </ul> <p>For each generation, save:</p> <ul> <li>Token logits</li> <li>Hidden states per layer</li> <li>Attention maps</li> <li>Cross-modal attention or image-token attention</li> <li>Image patch embeddings</li> <li>Output token probabilities</li> <li>Optional gradients if the model allows backward pass</li> </ul> <h3 id="step-3-define-internal-statistics">Step 3: Define internal statistics</h3> <p>Candidate features:</p> <h4 id="attention-based">Attention-based</h4> <ul> <li>Ratio of attention mass on image tokens vs. text tokens</li> <li>Attention entropy over image patches</li> <li>Attention concentration on object-relevant patches</li> <li>Attention sink ratio</li> <li>Layer-wise visual attention decay</li> <li>Head-level visual sensitivity</li> </ul> <h4 id="attribution-based">Attribution-based</h4> <ul> <li>Integrated gradients from image tokens to generated token logit</li> <li>Gradient × activation for image tokens</li> <li>Logit lens contribution from image-conditioned residual stream</li> <li>Difference between clean image and corrupted/blank image attribution</li> <li>Token-level causal mediation score</li> </ul> <h4 id="circuit-based">Circuit-based</h4> <ul> <li>Edge attribution from visual tokens to answer tokens</li> <li>Head-level causal importance</li> <li>Image-to-text circuit strength</li> <li>Prompt-to-answer circuit strength</li> <li>Ratio: visual circuit contribution / language-prior circuit contribution</li> <li>Circuit difference between hallucinated and grounded examples</li> </ul> <h4 id="representation-based">Representation-based</h4> <ul> <li>Linear probe accuracy for hallucination label</li> <li>CKA / CCA difference between hallucinated and grounded states</li> <li>Distance to object concept direction</li> <li>Hidden-state separability of correct vs. hallucinated object tokens</li> <li>Layer where hallucination becomes linearly decodable</li> </ul> <h4 id="decoding-based">Decoding-based</h4> <ul> <li>Logit gap between visual-conditioned and image-ablated forward pass</li> <li>Sensitivity to image corruption</li> <li>Sensitivity to prompt perturbation</li> <li>Token probability under image-present vs. image-removed settings</li> </ul> <h3 id="step-4-test-whether-statistics-separate-hallucination">Step 4: Test whether statistics separate hallucination</h3> <p>Train simple classifiers or use correlation analysis:</p> <ul> <li>Logistic regression</li> <li>Linear probe</li> <li>Random forest / XGBoost</li> <li>AUROC / AUPRC for hallucination detection</li> <li>Calibration analysis</li> <li>Cross-model generalization</li> <li>Cross-dataset generalization</li> <li>Cross-hallucination-type generalization</li> </ul> <p>The key is not just high detection performance, but interpretability:</p> <blockquote> <p>Which internal statistic best separates hallucinated from grounded generation, and at which layer/head/circuit does this happen?</p> </blockquote> <h2 id="possible-contribution">Possible contribution</h2> <p>A strong paper could contribute:</p> <ol> <li><strong>A taxonomy of internal hallucination mechanisms</strong> <ul> <li>Perception failure</li> <li>Grounding failure</li> <li>Language-prior override</li> <li>Reasoning failure</li> <li>Prompt-induced hallucination</li> </ul> </li> <li><strong>A set of mechanistic hallucination statistics</strong> <ul> <li>Attention grounding score</li> <li>Vision attribution ratio</li> <li>Circuit grounding ratio</li> <li>Representation separability score</li> <li>Visual sensitivity score</li> </ul> </li> <li><strong>Empirical finding</strong> <ul> <li>Some hallucination types are separable by attention patterns.</li> <li>Some are better separated by causal attribution.</li> <li>Reasoning hallucination may require representation/circuit analysis rather than raw attention.</li> </ul> </li> <li><strong>Mitigation direction</strong> <ul> <li>Use the discovered statistic as a training reward, decoding constraint, or intervention signal.</li> </ul> </li> </ol> <h2 id="possible-paper-structure">Possible paper structure</h2> <h3 id="1-introduction">1. Introduction</h3> <ul> <li>MLLMs hallucinate even when visual evidence is available.</li> <li>Most work evaluates or mitigates hallucination.</li> <li>Less is known about what changes internally during hallucination.</li> <li>We ask whether internal statistics can separate hallucinated and grounded generations.</li> </ul> <h3 id="2-related-work">2. Related Work</h3> <ul> <li>MLLM hallucination benchmarks</li> <li>Hallucination detection and mitigation</li> <li>Contrastive decoding and language-prior mitigation</li> <li>Mechanistic interpretability for VLMs</li> <li>Attribution and causal mediation methods</li> </ul> <h3 id="3-problem-setup">3. Problem Setup</h3> <p>Define a generation as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>y = M(image, prompt)
</code></pre></div></div> <p>For each generated token or span, assign a hallucination label:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>h_t = 1 if token/span is unsupported by image
h_t = 0 otherwise
</code></pre></div></div> <p>Goal:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>find internal statistic s_t such that s_t separates h_t = 1 from h_t = 0
</code></pre></div></div> <h3 id="4-method">4. Method</h3> <ul> <li>Collect hallucination-labeled examples.</li> <li>Run model and extract internal activations.</li> <li>Compute attention, attribution, circuit, and representation statistics.</li> <li>Analyze separability.</li> </ul> <h3 id="5-experiments">5. Experiments</h3> <p>Evaluation dimensions:</p> <ul> <li>Object hallucination</li> <li>Attribute hallucination</li> <li>Relation hallucination</li> <li>Reasoning hallucination</li> <li>Prompt-induced hallucination</li> </ul> <p>Evaluation metrics:</p> <ul> <li>AUROC for hallucination separation</li> <li>Correlation with hallucination severity</li> <li>Cross-dataset transfer</li> <li>Cross-model transfer</li> <li>Layer/head localization</li> </ul> <h3 id="6-analysis">6. Analysis</h3> <p>Key questions:</p> <ul> <li>Which layers first encode hallucination risk?</li> <li>Are hallucinated tokens less visually grounded?</li> <li>Do some attention heads systematically amplify prompt bias?</li> <li>Can causal ablation of identified heads reduce hallucination?</li> <li>Does representation separability appear before the hallucinated token is generated?</li> </ul> <h3 id="7-mitigation">7. Mitigation</h3> <p>Use discovered signals to intervene:</p> <ul> <li>Penalize low visual-attribution tokens during decoding.</li> <li>Rerank generations by visual grounding score.</li> <li>Ablate or downweight hallucination-associated heads.</li> <li>Train with reward favoring visual dependence.</li> <li>Add a contrastive loss between image-present and image-absent prompts.</li> </ul> <h2 id="what-is-novel-here">What is novel here?</h2> <p>The novelty is not simply “detect hallucination.” The stronger framing is:</p> <blockquote> <p>Build a mechanistic map of hallucination types and identify which internal statistics explain each type.</p> </blockquote> <p>A good research gap:</p> <ul> <li>Existing benchmarks say <strong>whether</strong> hallucination happened.</li> <li>Existing decoding methods reduce hallucination but may not explain <strong>why</strong>.</li> <li>Your project asks <strong>where and how hallucination emerges inside the model</strong>.</li> </ul> <h2 id="concrete-first-experiment">Concrete first experiment</h2> <p>Start with a narrow, publishable version:</p> <h3 id="experiment-object-hallucination-in-llava-style-models">Experiment: Object hallucination in LLaVA-style models</h3> <ol> <li>Use POPE or AMBER existence hallucination examples.</li> <li>Generate answers with an open-source LVLM.</li> <li>Extract layer-wise attention mass from generated object tokens to image tokens.</li> <li>Compute image ablation sensitivity:</li> </ol> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>visual_sensitivity = logit_original(object_token) - logit_image_ablated(object_token)
</code></pre></div></div> <ol> <li>Compute gradient attribution from image tokens to object-token logit.</li> <li>Train a linear probe to classify hallucinated vs. grounded object mentions.</li> <li>Analyze which layers/heads best separate the two classes.</li> <li>Ablate high-risk heads or apply visual-sensitivity decoding to test mitigation.</li> </ol> <p>This gives a clean first paper direction.</p> <h2 id="risks-and-limitations">Risks and limitations</h2> <ul> <li>Attention is not always a faithful explanation.</li> <li>Hallucination labels can be noisy.</li> <li>Open-ended generation makes span-level labeling difficult.</li> <li>Some hallucinations require world knowledge, not only image grounding.</li> <li>Attribution methods may be expensive for large MLLMs.</li> <li>Cross-model generalization may be weak.</li> </ul> <h2 id="possible-title-options">Possible title options</h2> <ul> <li><strong>Mechanistic Signatures of Hallucination in Multimodal Large Language Models</strong></li> <li><strong>When Vision-Language Models Hallucinate: Internal Statistics for Grounding Failure</strong></li> <li><strong>Do Multimodal Models See Before They Hallucinate? A Mechanistic Study</strong></li> <li><strong>Separating Grounded and Hallucinated Generations via Vision-Aware Internal Signals</strong></li> <li><strong>From Attention to Circuits: Understanding Hallucination in Multimodal Models</strong></li> </ul> <h2 id="key-references-to-start-from">Key references to start from</h2> <ul> <li> <p>Bai et al., <em>Hallucination of Multimodal Large Language Models: A Survey</em>, 2024.<br/> https://arxiv.org/abs/2404.18930</p> </li> <li> <p>Wang et al., <em>AMBER: An LLM-free Multi-dimensional Benchmark for MLLMs Hallucination Evaluation</em>, 2023.<br/> https://arxiv.org/abs/2311.07397</p> </li> <li> <p>Prabhakaran et al., <em>VADE: Visual Attention Guided Hallucination Detection and Mitigation in VLMs</em>, ACL Findings 2025.<br/> https://aclanthology.org/2025.findings-acl.773.pdf</p> </li> <li> <p>Gong et al., <em>DAMRO: Dive into the Attention Mechanism of LVLM to Reduce Object Hallucination</em>, EMNLP 2024.<br/> https://aclanthology.org/2024.emnlp-main.439.pdf</p> </li> <li> <p>Rudman et al., <em>Mechanisms of Prompt-Induced Hallucination in Vision-Language Models</em>, 2026.<br/> https://arxiv.org/abs/2601.05201</p> </li> <li> <p>Yang et al., <em>Understanding and Mitigating Hallucination in Large Vision-Language Models</em>, OpenReview.<br/> https://openreview.net/forum?id=Bjq4W7P2Us</p> </li> <li> <p>Leng et al. / related works on visual contrastive decoding for LVLM hallucination mitigation.</p> </li> </ul> <h2 id="one-sentence-pitch">One-sentence pitch</h2> <p>This project studies whether hallucinated and grounded multimodal generations can be separated by internal visual-grounding statistics, such as attention, attribution, circuit contribution, and representation geometry, thereby explaining when MLLMs hallucinate and providing signals for mitigation.</p>]]></content><author><name></name></author><category term="Idea"/><category term="Hallucination"/><summary type="html"><![CDATA[Idea]]></summary></entry><entry><title type="html">Hierarchical Reward for Long-Horizon Planning and Agent RL</title><link href="https://jefferyy-peng.github.io/blog/2026/Hierarchical/" rel="alternate" type="text/html" title="Hierarchical Reward for Long-Horizon Planning and Agent RL"/><published>2026-05-02T00:00:00+00:00</published><updated>2026-05-02T00:00:00+00:00</updated><id>https://jefferyy-peng.github.io/blog/2026/Hierarchical</id><content type="html" xml:base="https://jefferyy-peng.github.io/blog/2026/Hierarchical/"><![CDATA[<p><strong>TL;DR:</strong></p> <h1 id="research-idea-2-hierarchical-reward-for-long-horizon-planning-and-agent-rl">Research Idea 2: Hierarchical Reward for Long-Horizon Planning and Agent RL</h1> <h2 id="working-title">Working title</h2> <p><strong>Hierarchical Rewards for Long-Horizon Language Agents: Learning to Plan, Execute, Revise, and Credit Subgoals</strong></p> <h2 id="core-research-question">Core research question</h2> <p>Long-horizon agents often fail because they must solve tasks that require many dependent decisions. A single final success/failure reward is too sparse. The idea is to design a <strong>hierarchical reward structure</strong> where:</p> <ul> <li>A long-term goal is decomposed into high-level plans.</li> <li>Each high-level plan is decomposed into subgoals.</li> <li>Each subgoal is decomposed into executable steps.</li> <li>Rewards are assigned not only for final task success, but also for correct intermediate planning, subgoal completion, and plan revision.</li> <li>Higher-level goal completion can validate or invalidate lower-level plans.</li> </ul> <p>The central question is:</p> <blockquote> <p>Can hierarchical reward design improve long-horizon agent learning by giving credit to correct plans, correct subgoals, correct execution, and correct revision?</p> </blockquote> <h2 id="motivation">Motivation</h2> <p>In long-horizon tasks, final rewards are sparse and delayed. For example, in a travel-planning agent:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Goal: Plan a 5-day Cancun trip under budget and time constraints.

High-level plan:
1. Choose hotel route.
2. Schedule outdoor tours.
3. Arrange transport.
4. Check constraints.

Subgoals:
1. Find hotels.
2. Select tours.
3. Balance rest and activity.
4. Verify cost and timing.

Low-level actions:
1. Search hotel.
2. Search ferry.
3. Compare tour hours.
4. Update itinerary.
</code></pre></div></div> <p>If the final itinerary fails because one ferry time is impossible, standard RL may give a bad reward to the whole trajectory. But many parts of the plan may still be correct. A hierarchical reward can assign partial credit and identify which level failed.</p> <h2 id="relationship-to-existing-work">Relationship to existing work</h2> <h3 id="hierarchical-rl">Hierarchical RL</h3> <p>Traditional hierarchical reinforcement learning already studies temporal abstraction:</p> <ul> <li>Options framework</li> <li>MAXQ decomposition</li> <li>Feudal RL</li> <li>Subgoal discovery</li> <li>Skill learning</li> </ul> <p>These methods decompose long-horizon tasks into higher-level policies and lower-level actions. However, classical HRL usually assumes environment states/actions, not natural-language plans.</p> <h3 id="llm-agent-planning">LLM agent planning</h3> <p>Recent LLM-agent work uses explicit planning and decomposition:</p> <ul> <li>Plan-and-Act style frameworks separate a planner from an executor.</li> <li>Web-agent methods use high-level plans to guide low-level browser actions.</li> <li>TravelPlanner, WebArena, WebVoyager, DeepPlanning, and related benchmarks test long-horizon constrained planning.</li> <li>ReAct / Reflexion / Tree-of-Thought / plan-execute-replan methods use reasoning traces and feedback loops.</li> </ul> <p>But many such systems are prompt- or inference-time frameworks, not fully trained with hierarchical reward signals.</p> <h3 id="planning-benchmarks">Planning benchmarks</h3> <p>Relevant benchmarks include:</p> <ul> <li><strong>PlanBench</strong>: formal planning and reasoning about actions/change.</li> <li><strong>TravelPlanner</strong>: real-world travel planning with constraints and tools.</li> <li><strong>DeepPlanning</strong>: long-horizon planning with travel and shopping tasks.</li> <li><strong>WebArena / WebVoyager</strong>: web-navigation tasks requiring many steps.</li> <li><strong>Robotouille</strong>: asynchronous planning benchmark.</li> <li><strong>Flex-TravelPlanner</strong>: flexible planning with changing constraints.</li> <li><strong>REALM-Bench</strong>: real-world planning scenarios and adaptation to disruptions.</li> <li><strong>HeroBench</strong>: long-horizon hierarchical planning in an RPG-like world.</li> </ul> <p>These benchmarks are relevant because they can expose failures in decomposition, execution, constraint satisfaction, and replanning.</p> <h2 id="existing-design-patterns-close-to-your-idea">Existing design patterns close to your idea</h2> <h3 id="1-plan-and-execute-agents">1. Plan-and-execute agents</h3> <p>A model first generates a high-level plan, then another module executes it. The plan provides structure, but the system may still lack explicit reward for whether the plan hierarchy itself is correct.</p> <h3 id="2-replan--revise-loops">2. Replan / revise loops</h3> <p>Agents can revise the plan after observing failure. This is close to your idea, but many works treat revision as an inference-time heuristic rather than a trainable ability with explicit revision rewards.</p> <h3 id="3-verifier-guided-planning">3. Verifier-guided planning</h3> <p>A verifier checks whether a plan or step satisfies constraints. This can provide intermediate reward:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reward = final_success + constraint_satisfaction + verifier_score
</code></pre></div></div> <h3 id="4-process-reward-models">4. Process reward models</h3> <p>Instead of only rewarding final answer correctness, a process reward model scores intermediate reasoning steps. This idea can be adapted from math/code reasoning to long-horizon agent planning.</p> <h3 id="5-hierarchical-goalsubgoal-rewards">5. Hierarchical goal/subgoal rewards</h3> <p>A reward can be assigned at multiple levels:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_total = R_final + R_plan + R_subgoal + R_step + R_revision
</code></pre></div></div> <p>This is the direct version of your proposal.</p> <h2 id="proposed-method">Proposed method</h2> <p>Represent an agent trajectory as a hierarchy:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Goal G
 ├── Plan P1
 │    ├── Subgoal S1
 │    │    ├── Action a1
 │    │    └── Action a2
 │    └── Subgoal S2
 │         ├── Action a3
 │         └── Action a4
 └── Plan revision P2
      └── ...
</code></pre></div></div> <p>Each level receives a reward.</p> <h2 id="reward-design">Reward design</h2> <h3 id="1-final-task-reward">1. Final task reward</h3> <p>Measures whether the whole task succeeds.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_final = 1 if final task is completed else 0
</code></pre></div></div> <p>Examples:</p> <ul> <li>Correct travel itinerary satisfying all constraints</li> <li>Web task completed</li> <li>Shopping task completed</li> <li>Game/crafting goal achieved</li> <li>Robot task completed</li> </ul> <h3 id="2-high-level-plan-reward">2. High-level plan reward</h3> <p>Measures whether the proposed plan is valid and complete before execution.</p> <p>Possible criteria:</p> <ul> <li>Covers all required constraints</li> <li>Has feasible ordering</li> <li>Does not contain contradictions</li> <li>Decomposes the goal into meaningful subgoals</li> <li>Matches environment affordances</li> </ul> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_plan = verifier(plan, goal, constraints)
</code></pre></div></div> <h3 id="3-subgoal-reward">3. Subgoal reward</h3> <p>Measures whether each subgoal is achieved.</p> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_subgoal_i = 1 if subgoal_i is completed else 0
</code></pre></div></div> <p>For travel planning:</p> <ul> <li>Hotel chosen for every night</li> <li>Transportation selected</li> <li>Tour schedule feasible</li> <li>Budget satisfied</li> <li>Opening hours respected</li> </ul> <h3 id="4-stepaction-reward">4. Step/action reward</h3> <p>Measures whether each action contributes to the current subgoal.</p> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_action_t = usefulness(action_t, subgoal_i)
</code></pre></div></div> <p>This can be learned by a reward model or computed by an environment verifier.</p> <h3 id="5-revision-reward">5. Revision reward</h3> <p>Measures whether the agent correctly detects and fixes a plan failure.</p> <p>Possible criteria:</p> <ul> <li>Detects infeasible step</li> <li>Identifies the correct failed constraint</li> <li>Proposes minimal repair</li> <li>Preserves still-correct parts of the plan</li> <li>Improves final feasibility</li> </ul> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_revision = score(new_plan) - score(old_plan)
</code></pre></div></div> <p>This is especially important for your idea.</p> <h3 id="6-cross-level-consistency-reward">6. Cross-level consistency reward</h3> <p>Higher-level goals should validate lower-level subgoals.</p> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_consistency = 1 if all subgoals jointly satisfy the high-level goal else 0
</code></pre></div></div> <p>This prevents locally correct but globally inconsistent plans.</p> <h2 id="a-possible-total-reward">A possible total reward</h2> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R = λ_final R_final
  + λ_plan R_plan
  + λ_subgoal Σ_i R_subgoal_i
  + λ_action Σ_t R_action_t
  + λ_revision R_revision
  + λ_consistency R_consistency
</code></pre></div></div> <p>The key research issue is choosing weights and avoiding reward hacking.</p> <h2 id="training-setup">Training setup</h2> <p>Possible training pipeline:</p> <h3 id="stage-1-supervised-plan-decomposition">Stage 1: Supervised plan decomposition</h3> <p>Train the model to produce hierarchical plans from successful trajectories.</p> <p>Data format:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"goal"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"high_level_plan"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"subgoals"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"actions"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"revision"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h3 id="stage-2-verifier--reward-model-training">Stage 2: Verifier / reward model training</h3> <p>Train verifiers for:</p> <ul> <li>Plan feasibility</li> <li>Constraint satisfaction</li> <li>Subgoal completion</li> <li>Action usefulness</li> <li>Revision correctness</li> </ul> <p>These can be trained from:</p> <ul> <li>Environment outcomes</li> <li>Synthetic traces</li> <li>Human annotations</li> <li>LLM-as-judge labels</li> <li>Constraint solvers</li> </ul> <h3 id="stage-3-rl-fine-tuning">Stage 3: RL fine-tuning</h3> <p>Use PPO / GRPO / DPO-like preference optimization / actor-critic methods with hierarchical rewards.</p> <p>The model is rewarded for:</p> <ul> <li>Producing a valid plan</li> <li>Executing subgoals</li> <li>Revising when needed</li> <li>Achieving final success</li> </ul> <h2 id="datasets-for-plan-revision-or-flexible-planning">Datasets for plan revision or flexible planning</h2> <p>There are some relevant datasets/benchmarks, but the exact ability you describe is not fully solved.</p> <h3 id="useful-existing-benchmarks">Useful existing benchmarks</h3> <h4 id="travelplanner">TravelPlanner</h4> <p>Good for constrained real-world planning. It includes tool use, user constraints, commonsense constraints, and hard constraints. It can be adapted to revision by changing constraints mid-task or hiding information until later.</p> <h4 id="flex-travelplanner">Flex-TravelPlanner</h4> <p>Closer to plan revision because it evaluates flexible planning under changing constraints.</p> <h4 id="deepplanning">DeepPlanning</h4> <p>Useful for long-horizon global/local planning, especially travel and shopping. It can test whether an agent performs global optimization and local constraint reasoning.</p> <h4 id="webarena--webvoyager">WebArena / WebVoyager</h4> <p>Useful for long-horizon web execution. Revision can be tested when a page state changes or an earlier action fails.</p> <h4 id="planbench">PlanBench</h4> <p>Useful for formal plan validity and action precondition/effect reasoning. Plan repair can be created by perturbing valid plans.</p> <h4 id="robotouille">Robotouille</h4> <p>Useful for asynchronous planning, where actions may happen in parallel or with delays.</p> <h4 id="realm-bench">REALM-Bench</h4> <p>Useful for real-world planning scenarios, multi-agent planning, and adaptation to disruptions.</p> <h4 id="herobench">HeroBench</h4> <p>Useful for explicit hierarchical planning, multi-level dependencies, and very long action sequences.</p> <h3 id="gap">Gap</h3> <p>Most benchmarks test whether the final plan succeeds, but fewer provide dense labels for:</p> <ul> <li>Whether each subgoal is correct</li> <li>Whether each decomposition is valid</li> <li>Whether each revision is minimal and correct</li> <li>Whether the agent knows which level of the hierarchy failed</li> </ul> <p>This is a strong research opportunity.</p> <h2 id="possible-benchmark-contribution">Possible benchmark contribution</h2> <p>Create a benchmark called something like:</p> <p><strong>HierPlan-Revise</strong></p> <p>Each task includes:</p> <ol> <li>A goal</li> <li>A hierarchical gold plan</li> <li>Subgoal dependencies</li> <li>Hidden or changing constraints</li> <li>Execution feedback</li> <li>Required plan revision</li> <li>Final success criteria</li> <li>Labels for which subgoal failed</li> </ol> <p>Example task:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Goal: Plan a 4-day trip under $1200.

Initial plan:
- Day 1: Museum
- Day 2: Island tour
- Day 3: Ruins
- Day 4: Rest

New feedback:
- Ferry unavailable on Day 2.
- Ruins closed on Day 3.

Required behavior:
- Detect affected subgoals.
- Revise only the necessary days.
- Preserve valid hotel and budget choices.
- Produce final feasible plan.
</code></pre></div></div> <p>Evaluation:</p> <ul> <li>Plan validity</li> <li>Subgoal completion</li> <li>Revision minimality</li> <li>Constraint satisfaction</li> <li>Final success</li> <li>Token/action efficiency</li> <li>Error localization accuracy</li> </ul> <h2 id="possible-experiments">Possible experiments</h2> <h3 id="experiment-1-does-hierarchical-reward-improve-final-success">Experiment 1: Does hierarchical reward improve final success?</h3> <p>Compare:</p> <ul> <li>Sparse final reward only</li> <li>Final reward + subgoal reward</li> <li>Final reward + plan reward</li> <li>Final reward + subgoal + revision reward</li> <li>Full hierarchical reward</li> </ul> <p>Measure:</p> <ul> <li>Final success rate</li> <li>Constraint satisfaction</li> <li>Number of invalid actions</li> <li>Replanning success</li> <li>Efficiency</li> </ul> <h3 id="experiment-2-does-revision-reward-improve-recovery-from-failure">Experiment 2: Does revision reward improve recovery from failure?</h3> <p>Create tasks where the initial plan becomes invalid.</p> <p>Measure:</p> <ul> <li>Whether the agent detects the failure</li> <li>Whether it revises the correct subgoal</li> <li>Whether it avoids changing correct parts</li> <li>Whether final success improves</li> </ul> <h3 id="experiment-3-does-cross-level-reward-prevent-local-reward-hacking">Experiment 3: Does cross-level reward prevent local reward hacking?</h3> <p>Test whether agents optimize subgoals independently but violate the global goal.</p> <p>Example:</p> <ul> <li>Each day of travel plan looks good.</li> <li>But total budget exceeds limit.</li> <li>Or hotel location makes all tours impossible.</li> </ul> <p>Cross-level consistency reward should reduce this.</p> <h3 id="experiment-4-generalization-to-longer-horizons">Experiment 4: Generalization to longer horizons</h3> <p>Train on shorter tasks and test on longer tasks.</p> <p>Measure:</p> <ul> <li>Decomposition depth generalization</li> <li>Subgoal dependency handling</li> <li>Failure recovery</li> <li>Reward hacking</li> </ul> <h2 id="novelty-angle">Novelty angle</h2> <p>The novelty is not just “use hierarchical planning.” The stronger idea is:</p> <blockquote> <p>Use explicit hierarchical reward credit assignment to train agents to decompose, execute, verify, and revise long-horizon plans.</p> </blockquote> <p>Compared with existing work:</p> <ul> <li>Plan-and-act gives structure but not necessarily learned hierarchical reward.</li> <li>Process reward models score reasoning steps but are usually not tied to executable subgoal hierarchies.</li> <li>HRL gives temporal abstraction but not natural-language plan revision.</li> <li>Planning benchmarks test final plans but often lack dense hierarchical supervision.</li> </ul> <h2 id="possible-paper-structure">Possible paper structure</h2> <h3 id="1-introduction">1. Introduction</h3> <ul> <li>Long-horizon agents suffer from sparse rewards and compounding errors.</li> <li>Planning helps, but plans can be wrong or require revision.</li> <li>Current systems often lack explicit credit assignment across plan levels.</li> <li>We propose hierarchical rewards for plan, subgoal, action, and revision quality.</li> </ul> <h3 id="2-related-work">2. Related Work</h3> <ul> <li>Hierarchical RL</li> <li>LLM planning and agent benchmarks</li> <li>Process reward models</li> <li>Plan verification and plan repair</li> <li>Long-horizon web/travel/embodied agents</li> </ul> <h3 id="3-problem-formulation">3. Problem Formulation</h3> <p>Define a task as:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T = (G, C, E)
</code></pre></div></div> <p>where:</p> <ul> <li><code class="language-plaintext highlighter-rouge">G</code> is the goal</li> <li><code class="language-plaintext highlighter-rouge">C</code> is a set of constraints</li> <li><code class="language-plaintext highlighter-rouge">E</code> is the environment</li> </ul> <p>The agent produces a hierarchy:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>H = (P, S, A, R)
</code></pre></div></div> <p>where:</p> <ul> <li><code class="language-plaintext highlighter-rouge">P</code> is the high-level plan</li> <li><code class="language-plaintext highlighter-rouge">S</code> are subgoals</li> <li><code class="language-plaintext highlighter-rouge">A</code> are actions</li> <li><code class="language-plaintext highlighter-rouge">R</code> are revisions</li> </ul> <h3 id="4-hierarchical-reward">4. Hierarchical Reward</h3> <p>Define reward at each level:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_total = R_final + R_plan + R_subgoal + R_action + R_revision + R_consistency
</code></pre></div></div> <h3 id="5-benchmark--data-construction">5. Benchmark / Data Construction</h3> <p>Use existing benchmarks or create perturbations:</p> <ul> <li>TravelPlanner with changing constraints</li> <li>PlanBench with invalidated action preconditions</li> <li>WebArena tasks with execution failures</li> <li>Synthetic hierarchical tasks with ground-truth subgoal trees</li> </ul> <h3 id="6-training">6. Training</h3> <ul> <li>SFT on successful hierarchical traces</li> <li>Train verifiers / reward models</li> <li>RL with hierarchical reward</li> <li>Optional GRPO/PPO optimization</li> </ul> <h3 id="7-experiments">7. Experiments</h3> <ul> <li>Compare reward designs</li> <li>Analyze plan revision</li> <li>Analyze long-horizon scaling</li> <li>Analyze reward hacking</li> <li>Evaluate cross-domain transfer</li> </ul> <h3 id="8-analysis">8. Analysis</h3> <p>Key questions:</p> <ul> <li>Which reward level contributes most?</li> <li>Does subgoal reward help or hurt?</li> <li>Does revision reward improve recovery?</li> <li>Does plan reward improve global coherence?</li> <li>Does hierarchical reward generalize to longer tasks?</li> </ul> <h2 id="risks-and-limitations">Risks and limitations</h2> <ul> <li>Reward hacking: agent may optimize subgoals without solving global task.</li> <li>Verifier quality: bad verifier gives misleading rewards.</li> <li>Annotation cost: hierarchical gold plans are expensive.</li> <li>Credit assignment: hard to know which subgoal caused final failure.</li> <li>Over-planning: too much planning may reduce efficiency.</li> <li>Domain specificity: travel/web planning rewards may not transfer to embodied tasks.</li> </ul> <h2 id="concrete-first-project">Concrete first project</h2> <p>A practical first paper:</p> <h3 id="hierarchical-reward-for-travelplanner-style-revision">Hierarchical reward for TravelPlanner-style revision</h3> <ol> <li>Start from TravelPlanner or a similar constrained planning benchmark.</li> <li>Create perturbed tasks where one or more constraints change after initial planning.</li> <li>Ask the agent to: <ul> <li>generate a plan,</li> <li>receive feedback,</li> <li>localize the failed subgoal,</li> <li>revise the plan,</li> <li>produce final answer.</li> </ul> </li> <li>Define rewards: <ul> <li>final pass rate,</li> <li>constraint satisfaction,</li> <li>subgoal correctness,</li> <li>revision minimality,</li> <li>failure localization.</li> </ul> </li> <li>Train or tune an agent with hierarchical reward.</li> <li>Compare against: <ul> <li>final reward only,</li> <li>ReAct,</li> <li>Reflexion,</li> <li>Plan-and-Act,</li> <li>verifier-only reranking.</li> </ul> </li> </ol> <p>This is a clean and publishable version of your idea.</p> <h2 id="key-references-to-start-from">Key references to start from</h2> <ul> <li> <p>Sutton, Precup, and Singh, <em>Between MDPs and Semi-MDPs: A Framework for Temporal Abstraction in Reinforcement Learning</em>, 1999.<br/> https://www.sciencedirect.com/science/article/pii/S0004370299000521</p> </li> <li> <p>Dietterich, <em>Hierarchical Reinforcement Learning with the MAXQ Value Function Decomposition</em>, 2000.<br/> https://www.jair.org/index.php/jair/article/view/10266</p> </li> <li> <p>Valmeekam et al., <em>PlanBench: An Extensible Benchmark for Evaluating Large Language Models on Planning and Reasoning about Change</em>, NeurIPS 2023.<br/> https://arxiv.org/abs/2206.10498</p> </li> <li> <p>Xie et al., <em>TravelPlanner: A Benchmark for Real-World Planning with Language Agents</em>, ICML 2024.<br/> https://arxiv.org/abs/2402.01622</p> </li> <li> <p>Erdogan et al., <em>Improving Planning of Agents for Long-Horizon Tasks / Plan-and-Act</em>, ICML 2025.<br/> https://arxiv.org/abs/2503.09572</p> </li> <li> <p>DeepPlanning benchmark, 2026.<br/> https://qwenlm.github.io/Qwen-Agent/en/benchmarks/deepplanning/</p> </li> <li> <p>Flex-TravelPlanner, 2025.<br/> https://openreview.net/forum?id=a7unQ5jMx7</p> </li> <li> <p>HeroBench, 2026.<br/> https://arxiv.org/html/2508.12782v2</p> </li> </ul> <h2 id="one-sentence-pitch">One-sentence pitch</h2> <p>This project proposes hierarchical reward credit assignment for long-horizon language agents, rewarding not only final task success but also plan validity, subgoal completion, action usefulness, failure localization, and plan revision.</p> <hr/> <h1 id="addendum-counterfactual-world-model-evaluation-for-long-horizon-agents">Addendum: Counterfactual World-Model Evaluation for Long-Horizon Agents</h1> <h2 id="new-idea">New idea</h2> <p>A natural extension of the hierarchical reward idea is to ask whether an agent has a <strong>counterfactual world model</strong>.</p> <p>The key question is:</p> <blockquote> <p>Given the current world state or trajectory, can the model reason about what would happen if one condition, action, observation, or plan step were different?</p> </blockquote> <p>This is closely related to world modeling, but the focus is more specific:</p> <ul> <li>Not just “can the agent complete the task?”</li> <li>Not just “can the agent predict the next state?”</li> <li>Not just “can the agent revise after failure?”</li> <li>But: <strong>can the agent mentally simulate alternative trajectories and compare their downstream consequences?</strong></li> </ul> <p>In other words, the model should answer questions like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Given the current trajectory τ:
- What would happen if action a_t were replaced by a'_t?
- What if observation o_t had been different?
- What if subgoal S_i were skipped?
- What if the high-level plan order were changed?
- What if a constraint appeared earlier?
- Which future failure would this alternative plan cause?
- Which plan revision would minimally fix the trajectory?
</code></pre></div></div> <p>This ability can be called:</p> <p><strong>Counterfactual Trajectory Reasoning</strong><br/> or<br/> <strong>Counterfactual World-Modeling for Agents</strong></p> <h2 id="why-this-sounds-like-a-world-model">Why this sounds like a world model</h2> <p>A world model usually predicts environment dynamics:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>current state + action -&gt; next state / future trajectory
</code></pre></div></div> <p>A counterfactual world model asks a stronger question:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>current trajectory + intervention -&gt; alternative future trajectory
</code></pre></div></div> <p>So the agent is not only predicting the next factual state. It is simulating a hypothetical branch of the world.</p> <p>For long-horizon planning, this is essential because good agents must reason about delayed consequences:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>If I choose this hotel today, then tomorrow's tour becomes impossible.
If I skip this information-gathering step, I may not know whether the final answer is valid.
If I change this subgoal, the later transportation plan must also change.
</code></pre></div></div> <p>This connects naturally to hierarchical planning because counterfactuals can occur at multiple levels:</p> <table> <thead> <tr> <th>Level</th> <th>Counterfactual question</th> </tr> </thead> <tbody> <tr> <td>Goal level</td> <td>What if the user changed the objective?</td> </tr> <tr> <td>Constraint level</td> <td>What if the budget/time/location constraint changed?</td> </tr> <tr> <td>Plan level</td> <td>What if we used a different high-level plan?</td> </tr> <tr> <td>Subgoal level</td> <td>What if this subgoal were skipped or reordered?</td> </tr> <tr> <td>Action level</td> <td>What if this tool/action were replaced?</td> </tr> <tr> <td>Observation level</td> <td>What if the agent observed a different result?</td> </tr> <tr> <td>Revision level</td> <td>What if the agent repaired the wrong part of the plan?</td> </tr> </tbody> </table> <h2 id="deeper-related-work-search">Deeper related work search</h2> <p>The idea is related to several existing research lines, but it is not fully covered by any single one.</p> <h3 id="1-counterfactual-reasoning-benchmarks-for-llms">1. Counterfactual reasoning benchmarks for LLMs</h3> <p>There are benchmarks such as <strong>CounterBench</strong>, which evaluate whether LLMs can perform counterfactual reasoning over causal graphs and structured questions. CounterBench includes different counterfactual types such as basic, joint, conditional, nested, and backdoor counterfactuals.</p> <p>However, this is mostly about <strong>static causal reasoning</strong> rather than interactive agent trajectories.</p> <p>Gap:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CounterBench tests whether a model can answer counterfactual causal questions.
It does not directly test whether an agent can simulate how an alternative action or observation changes a long-horizon task trajectory.
</code></pre></div></div> <h3 id="2-forward-counterfactual-generation">2. Forward counterfactual generation</h3> <p>Some work studies <strong>forward counterfactual reasoning</strong>, where the model predicts what future developments would follow from an alternative condition. For example, FIN-FORCE studies forward counterfactual generation for financial news.</p> <p>This is conceptually close because it asks “what would happen next if something were different?”</p> <p>Gap:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Forward counterfactual generation focuses on future scenario generation, often in a domain like finance.
It is not primarily an agent benchmark with actions, observations, tool use, subgoal dependencies, and executable plan revision.
</code></pre></div></div> <h3 id="3-counterfactuals-for-language-model-agents">3. Counterfactuals for language model agents</h3> <p>Recent work on <strong>Abstract Counterfactuals for Language Model Agents</strong> proposes counterfactual reasoning at the level of high-level action semantics instead of raw token-level interventions. This is very relevant because it argues that action counterfactuals should be represented at an abstract semantic level.</p> <p>Gap:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This work studies how to construct meaningful counterfactual actions for LM agents.
It does not provide a broad benchmark for hierarchical long-horizon plan counterfactuals or evaluate whether agents can predict downstream trajectory consequences across plan/subgoal/action levels.
</code></pre></div></div> <h3 id="4-counterfactual-trajectory-training-in-navigation">4. Counterfactual trajectory training in navigation</h3> <p>Older embodied AI work such as <strong>Counterfactual Vision-and-Language Navigation</strong> uses counterfactual observations and trajectories to improve robustness in navigation. It explicitly asks questions like what would happen if a different object were observed.</p> <p>Gap:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This is close in spirit, but it is domain-specific to vision-and-language navigation and mainly used as a training strategy for generalization.
It is not a general agent benchmark for counterfactual world models across web, travel, tool-use, planning, or hierarchical task domains.
</code></pre></div></div> <h3 id="5-world-model-benchmarks">5. World-model benchmarks</h3> <p>Recent work on world-model evaluation argues that world models should be evaluated not only by visual fidelity or next-state prediction, but also by prediction, planning, and counterfactual reasoning. For example:</p> <ul> <li><strong>AutumnBench / WorldTest</strong> evaluates world-model learning using interactive grid-world environments and includes prediction, planning, and counterfactual reasoning tasks.</li> <li><strong>World Reasoning Arena (WR-Arena)</strong> evaluates world models on action simulation fidelity, long-horizon forecast, and simulative reasoning/planning.</li> <li>Recent surveys on agentic world modeling define world models as learning state-transition dynamics and helping agents anticipate consequences of candidate actions.</li> </ul> <p>Gap:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>These works evaluate world-model capabilities, but often focus on world-model systems or simulated environments.
The proposed idea targets LLM agents specifically and asks whether their internal or external reasoning can support counterfactual trajectory evaluation over natural-language hierarchical plans.
</code></pre></div></div> <h3 id="6-planning-analysis-for-llm-agents">6. Planning analysis for LLM agents</h3> <p>Recent planning-centric analyses argue that LLM agents often behave like step-wise greedy policies and fail because early actions do not account for delayed consequences. This directly supports the need for future-aware and counterfactual evaluation.</p> <p>Gap:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>These works show long-horizon planning failure and propose lookahead/value-estimation methods.
But they do not necessarily isolate counterfactual world-model competence as a benchmarked capability:
given trajectory τ and intervention do(a_t = a'_t), can the model predict the alternative outcome?
</code></pre></div></div> <h3 id="7-counterfactual-trajectory-pairs-for-agent-training">7. Counterfactual trajectory pairs for agent training</h3> <p>CaRT uses counterfactual trajectory pairs to teach LLM agents when to stop gathering information. It creates paired trajectories where termination is appropriate vs. minimally modified trajectories where termination is not.</p> <p>Gap:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CaRT is highly relevant, but it focuses on the specific skill of termination / information-gathering.
A broader benchmark could evaluate counterfactual reasoning over arbitrary actions, observations, plan steps, subgoals, constraints, and revisions.
</code></pre></div></div> <h2 id="novelty-assessment">Novelty assessment</h2> <p>The broad phrase <strong>“counterfactual world model” is not completely new</strong>. There is existing work on:</p> <ul> <li>Counterfactual reasoning in LLMs</li> <li>Counterfactual planning</li> <li>Counterfactual trajectories in navigation</li> <li>World-model benchmarks with counterfactual tasks</li> <li>LLM-as-world-model planning methods</li> <li>Counterfactual trajectory pairs for specific agent skills</li> </ul> <p>So the paper should not claim:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>No one has studied counterfactual reasoning or world models before.
</code></pre></div></div> <p>A safer and stronger novelty claim is:</p> <blockquote> <p>Existing agent benchmarks mostly evaluate final task success or factual trajectory execution. Existing counterfactual benchmarks often focus on static causal reasoning, domain-specific future generation, or simulated world-model evaluation. What remains underexplored is a benchmark and training framework for <strong>counterfactual trajectory reasoning in long-horizon LLM agents</strong>, especially across hierarchical plan levels: goal, constraint, plan, subgoal, action, observation, and revision.</p> </blockquote> <p>This is a more defensible research gap.</p> <h2 id="proposed-research-direction">Proposed research direction</h2> <h3 id="main-task">Main task</h3> <p>Given a factual agent trajectory:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>τ = (s_0, a_0, o_1, a_1, o_2, ..., a_T, o_T)
</code></pre></div></div> <p>and an intervention:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>do(x_i = x'_i)
</code></pre></div></div> <p>where <code class="language-plaintext highlighter-rouge">x_i</code> may be an action, observation, constraint, subgoal, or plan step, ask the model to predict:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>τ' = alternative future trajectory
</code></pre></div></div> <p>and/or answer:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Will the final task still succeed?
Which future step changes?
Which constraint will fail?
Which subgoal becomes invalid?
What minimal revision restores success?
</code></pre></div></div> <h3 id="example-travel-planning">Example: travel planning</h3> <p>Factual trajectory:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Goal: Plan a 4-day Cancun trip.
Plan:
Day 1: Isla Mujeres
Day 2: Chichen Itza + cenote
Day 3: Xplor
Day 4: Cozumel
</code></pre></div></div> <p>Counterfactual intervention:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>What if the ferry to Cozumel is unavailable on Day 4?
</code></pre></div></div> <p>Expected model behavior:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>- Recognize that only the Cozumel subgoal is directly affected.
- Predict downstream effects on transport, hotel location, and timing.
- Preserve unaffected days if still valid.
- Propose a minimal repair, e.g., swap Cozumel with Isla Mujeres or replace Cozumel with a local activity.
</code></pre></div></div> <h3 id="example-web-agent">Example: web agent</h3> <p>Factual trajectory:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Goal: Buy the cheapest compatible laptop charger.
The agent searches product page A, filters by price, and purchases item X.
</code></pre></div></div> <p>Counterfactual intervention:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>What if the agent had clicked the compatibility tab before purchasing?
</code></pre></div></div> <p>Expected behavior:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>- Predict that the agent would discover item X is incompatible.
- Predict that the original purchase should be avoided.
- Identify which earlier decision changes.
- Continue with an alternative search path.
</code></pre></div></div> <h3 id="example-tool-use-agent">Example: tool-use agent</h3> <p>Factual trajectory:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The agent answers a question after one web search.
</code></pre></div></div> <p>Counterfactual intervention:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>What if the first search result were outdated?
</code></pre></div></div> <p>Expected behavior:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>- Predict that the confidence of the answer should decrease.
- Continue searching or verify with a more recent source.
- Avoid finalizing too early.
</code></pre></div></div> <h2 id="benchmark-design-counterfactual-agent-world-model-benchmark">Benchmark design: Counterfactual Agent World Model Benchmark</h2> <p>A possible benchmark name:</p> <p><strong>CAWM-Bench: Counterfactual Agent World-Model Benchmark</strong></p> <p>or</p> <p><strong>CoTrajBench: Counterfactual Trajectory Reasoning Benchmark</strong></p> <p>or</p> <p><strong>Hier-CFBench: Hierarchical Counterfactual Planning Benchmark</strong></p> <h3 id="data-format">Data format</h3> <p>Each example contains:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"goal"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"constraints"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"factual_plan"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"factual_trajectory"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"intervention"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"action | observation | constraint | subgoal | plan_step | revision"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"target"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
    </span><span class="nl">"replacement"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"expected_counterfactual_effects"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"expected_final_outcome"</span><span class="p">:</span><span class="w"> </span><span class="s2">"success | failure | changed_success"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"minimal_revision"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"affected_subgoals"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"unaffected_subgoals"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h3 id="intervention-types">Intervention types</h3> <ol> <li><strong>Action counterfactual</strong> <ul> <li>What if the agent used a different tool/action?</li> </ul> </li> <li><strong>Observation counterfactual</strong> <ul> <li>What if the tool returned different information?</li> </ul> </li> <li><strong>Constraint counterfactual</strong> <ul> <li>What if a new constraint appeared?</li> </ul> </li> <li><strong>Subgoal counterfactual</strong> <ul> <li>What if a subgoal were skipped, reordered, or replaced?</li> </ul> </li> <li><strong>Plan-level counterfactual</strong> <ul> <li>What if the high-level strategy were different?</li> </ul> </li> <li><strong>Revision counterfactual</strong> <ul> <li>What if the agent repaired the wrong part of the plan?</li> </ul> </li> <li><strong>Information-gathering counterfactual</strong> <ul> <li>What if the agent stopped earlier or searched one more time?</li> </ul> </li> </ol> <h2 id="evaluation-metrics">Evaluation metrics</h2> <h3 id="1-outcome-prediction-accuracy">1. Outcome prediction accuracy</h3> <p>Can the model correctly predict whether the counterfactual trajectory succeeds or fails?</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Acc(success/failure)
</code></pre></div></div> <h3 id="2-affected-subgoal-localization">2. Affected-subgoal localization</h3> <p>Can it identify which subgoals are affected by the intervention?</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>F1(affected_subgoals)
</code></pre></div></div> <h3 id="3-unaffected-subgoal-preservation">3. Unaffected-subgoal preservation</h3> <p>Can it avoid unnecessarily changing valid parts of the plan?</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Preservation score
</code></pre></div></div> <h3 id="4-counterfactual-consistency">4. Counterfactual consistency</h3> <p>Does the predicted alternative trajectory logically follow from the intervention?</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>consistency(intervention, predicted_trajectory)
</code></pre></div></div> <h3 id="5-minimal-repair-score">5. Minimal repair score</h3> <p>If the counterfactual causes failure, can the model propose the smallest valid revision?</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>minimal_repair_score = validity - unnecessary_changes
</code></pre></div></div> <h3 id="6-long-horizon-dependency-accuracy">6. Long-horizon dependency accuracy</h3> <p>Can the model track delayed effects several steps later?</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>accuracy_by_distance_from_intervention
</code></pre></div></div> <h3 id="7-causal-contrast-score">7. Causal contrast score</h3> <p>Can the model explicitly contrast factual and counterfactual outcomes?</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Δ = predicted_outcome(τ') - predicted_outcome(τ)
</code></pre></div></div> <h2 id="training-with-hierarchical-counterfactual-reward">Training with hierarchical counterfactual reward</h2> <p>This addendum also strengthens the hierarchical reward idea.</p> <p>Instead of only rewarding factual task completion:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_final
</code></pre></div></div> <p>we can add counterfactual rewards:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_counterfactual =
  R_outcome_prediction
+ R_affected_subgoal
+ R_minimal_revision
+ R_counterfactual_consistency
+ R_unaffected_preservation
</code></pre></div></div> <p>Then the total hierarchical reward becomes:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_total =
  R_final
+ R_plan
+ R_subgoal
+ R_action
+ R_revision
+ R_consistency
+ R_counterfactual
</code></pre></div></div> <p>This encourages the agent not only to execute the current plan, but also to understand why the plan works and what would break if the world changed.</p> <h2 id="why-this-is-useful">Why this is useful</h2> <p>A counterfactual world-model benchmark would test abilities that normal success-rate benchmarks miss:</p> <table> <thead> <tr> <th>Normal agent benchmark</th> <th>Counterfactual world-model benchmark</th> </tr> </thead> <tbody> <tr> <td>Did the agent complete the task?</td> <td>Does the agent know why the task succeeded?</td> </tr> <tr> <td>Did the agent recover after failure?</td> <td>Could the agent predict the failure before executing?</td> </tr> <tr> <td>Did the final answer satisfy constraints?</td> <td>Does the agent know which constraint would fail under an alternative?</td> </tr> <tr> <td>Did the agent use tools correctly?</td> <td>Does the agent know when a different tool result would change the plan?</td> </tr> <tr> <td>Did the plan work once?</td> <td>Does the agent understand the space of nearby possible plans?</td> </tr> </tbody> </table> <h2 id="strong-paper-framing">Strong paper framing</h2> <p>The paper can be framed as:</p> <blockquote> <p>Current agent benchmarks evaluate realized behavior, but not counterfactual competence. We propose evaluating whether LLM agents possess a counterfactual world model: the ability to predict how alternative actions, observations, constraints, or plan steps would change future trajectory outcomes.</p> </blockquote> <p>This is highly aligned with your original hierarchical reward idea because:</p> <ul> <li>hierarchical plans define the structure of possible interventions;</li> <li>counterfactual reasoning tests whether the model understands dependencies between levels;</li> <li>counterfactual reward provides dense supervision for planning and revision;</li> <li>minimal repair tests whether the model can revise plans without destroying valid subgoals.</li> </ul> <h2 id="most-promising-concrete-version">Most promising concrete version</h2> <p>A first publishable version could focus on constrained planning:</p> <h3 id="dataset">Dataset</h3> <p>Use or extend:</p> <ul> <li>TravelPlanner</li> <li>Flex-TravelPlanner</li> <li>WebArena-style tasks</li> <li>PlanBench-style symbolic plans</li> <li>Synthetic hierarchical planning tasks</li> </ul> <h3 id="perturbation-generation">Perturbation generation</h3> <p>For each factual successful trajectory, generate counterfactual interventions:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>change one constraint
change one observation
replace one action
remove one subgoal
swap two plan steps
invalidate one tool result
</code></pre></div></div> <h3 id="labels">Labels</h3> <p>Automatically or semi-automatically label:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>success/failure under counterfactual
affected subgoals
required repair
minimal valid revised plan
</code></pre></div></div> <h3 id="baselines">Baselines</h3> <p>Compare:</p> <ul> <li>Direct prompting</li> <li>Chain-of-thought</li> <li>ReAct</li> <li>Reflexion</li> <li>Plan-and-Act</li> <li>Tree-of-thought / MCTS-style planning</li> <li>Agent with explicit world model</li> <li>Agent trained with hierarchical counterfactual reward</li> </ul> <h3 id="main-claim">Main claim</h3> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Agents with stronger counterfactual trajectory reasoning should perform better at long-horizon planning, plan revision, and robustness under distribution shift.
</code></pre></div></div> <h2 id="updated-one-sentence-pitch">Updated one-sentence pitch</h2> <p>This project proposes hierarchical reward learning for long-horizon agents, extended with <strong>counterfactual world-model evaluation</strong>: testing whether agents can predict how alternative actions, observations, constraints, or plan steps would change future trajectory outcomes and use that knowledge to revise plans minimally and correctly.</p> <h2 id="references-for-this-addendum">References for this addendum</h2> <ul> <li> <p>Chen et al., <strong>CounterBench: Evaluating and Improving Counterfactual Reasoning in Large Language Models</strong>, AAAI 2026.<br/> https://arxiv.org/abs/2502.11008</p> </li> <li> <p>Ong et al., <strong>A Benchmark for Forward Counterfactual Generation</strong>, EMNLP 2025.<br/> https://aclanthology.org/2025.emnlp-main.575/</p> </li> <li> <p>Warrier et al., <strong>Benchmarking World-Model Learning / AutumnBench</strong>, 2025.<br/> https://arxiv.org/abs/2510.19788</p> </li> <li> <p><strong>World Reasoning Arena: A Benchmark for Next-Generation World Models</strong>, 2026.<br/> https://arxiv.org/abs/2603.25887</p> </li> <li> <p>Anonymous / arXiv, <strong>Agentic World Modeling: Foundations, Capabilities, Laws, and Beyond</strong>, 2026.<br/> https://arxiv.org/abs/2604.22748</p> </li> <li> <p>Parvaneh et al., <strong>Counterfactual Vision-and-Language Navigation</strong>, NeurIPS 2020.<br/> https://proceedings.neurips.cc/paper/2020/hash/39016cfe079db1bfb359ca72fcba3fd8-Abstract.html</p> </li> <li> <p>Qiao et al., <strong>Agent Planning with World Knowledge Model</strong>, NeurIPS 2024.<br/> https://proceedings.neurips.cc/paper_files/paper/2024/hash/d032263772946dd5026e7f3cd22bce5b-Abstract-Conference.html</p> </li> <li> <p><strong>Abstract Counterfactuals for Language Model Agents</strong>, 2025.<br/> https://arxiv.org/abs/2506.02946</p> </li> <li> <p><strong>CaRT: Teaching LLM Agents to Know When They Know Enough</strong>, 2025.<br/> https://arxiv.org/abs/2510.08517</p> </li> <li> <p><strong>Why Reasoning Fails to Plan: A Planning-Centric Analysis of Long-Horizon Decision Making in LLM Agents</strong>, 2026.<br/> https://arxiv.org/abs/2601.22311</p> </li> </ul>]]></content><author><name></name></author><category term="Idea"/><category term="RL"/><summary type="html"><![CDATA[Idea]]></summary></entry><entry><title type="html">Counterfactual Reasoning of Agents</title><link href="https://jefferyy-peng.github.io/blog/2026/cfr/" rel="alternate" type="text/html" title="Counterfactual Reasoning of Agents"/><published>2026-05-02T00:00:00+00:00</published><updated>2026-05-02T00:00:00+00:00</updated><id>https://jefferyy-peng.github.io/blog/2026/cfr</id><content type="html" xml:base="https://jefferyy-peng.github.io/blog/2026/cfr/"><![CDATA[<p><strong>TL;DR:</strong></p> <h1 id="counterfactual-reasoning-of-agents">Counterfactual Reasoning of Agents</h1> <h2 id="core-research-question">Core research question</h2> <p>Current agent benchmarks tell us whether an agent succeeded, but not whether the agent understands why it succeeded or failed. Does an agent have a counterfactual world model of its own trajectory?</p> <p>That means: after seeing an agent’s plan, observations, actions, tool outputs, and final result, can the model reason:</p> <p>If this action/observation/tool result/plan step had been different, what would change later? Would the final task still succeed? Which subgoal would break? What is the smallest repair?</p> <h2 id="motivation">Motivation</h2> <h3 id="1-final-success-is-a-weak-signal">1. Final success is a weak signal</h3> <p>Most agent benchmarks evaluate:</p> <p>Did the agent complete the task? yes/no</p> <p>But this hides many important differences.</p> <p>Two agents may both succeed, but one may understand the task better:</p> <p>Agent A: Finds the right answer by luck.</p> <p>Agent B: Finds the right answer because it verified sources, tracked constraints, and understood dependencies.</p> <p>Similarly, two agents may both fail, but for different reasons:</p> <p>Agent A: Failed because one tool result was wrong.</p> <p>Agent B: Failed because it did not understand which subgoal depended on that tool result.</p> <p>A success/failure benchmark cannot distinguish these cases.</p> <h3 id="2-long-horizon-agents-need-counterfactual-reasoning">2. Long-horizon agents need counterfactual reasoning</h3> <p>For short tasks, reactive behavior may be enough.</p> <p>For long-horizon tasks, agents must reason:</p> <p>If I choose this action now, what happens later? If this observation is wrong, which downstream decision changes? If this tool result changes, should I continue searching or start synthesis? If this plan step is removed, what risk increases? If this subgoal fails, can the parent goal still succeed?</p> <p>This is exactly counterfactual reasoning over trajectories.</p> <p>Without this ability, an agent may look competent in one trajectory but fail badly when anything changes.</p> <h3 id="3-real-world-agents-operate-under-partial-information">3. Real-world agents operate under partial information</h3> <p>In real tasks, the agent often does not know the full environment state.</p> <p>Examples:</p> <p>A hotel may be unavailable. A paper search result may be incomplete. A website field may be required but hidden. A product may be incompatible. A source may be outdated. A form may fail after submission.</p> <p>Good agents must reason over hidden or uncertain system states.</p> <p>Your benchmark tests whether the agent can say:</p> <p>This observation changes my belief. This subgoal is now invalid. This previous plan step must be revised. This final answer is no longer reliable.</p> <p>That is closer to real deployment than only checking final task success.</p> <h3 id="5-this-helps-diagnose-agent-failures">5. This helps diagnose agent failures</h3> <p>When an agent fails, we want to know:</p> <p>Was the failure caused by a wrong action? A misleading observation? A missing verification step? A bad tool result? A failed subgoal? A wrong high-level plan?</p> <p>Your benchmark forces the model to identify:</p> <p>first divergent step affected subgoals failure cause final outcome change minimal repair</p> <p>So it becomes a diagnostic tool, not just a leaderboard.</p> <p>This is valuable for both research and deployment.</p> <h3 id="6-this-connects-naturally-to-plan-repair">6. This connects naturally to plan repair</h3> <p>A strong agent should not only know that a trajectory fails.</p> <p>It should know how to minimally fix it.</p> <p>Example:</p> <p>The agent failed because it submitted the form before filling the required field.</p> <p>A weak model says:</p> <p>Try again.</p> <p>A stronger model says:</p> <p>Insert a required-field check before submission, fill the missing field, then submit again.</p> <p>This is exactly what real agents need: localized repair, not full restart.</p> <p>Your benchmark can measure this with:</p> <p>minimal repair validity minimal repair edit distance unaffected-step preservation</p> <p>That makes the benchmark practically useful.</p> <h3 id="7-this-can-support-better-training-signals">7. This can support better training signals</h3> <p>Your hierarchical reward idea becomes stronger with counterfactual trajectory data.</p> <p>Instead of only rewarding final success:</p> <p>R_final</p> <p>you can reward:</p> <p>R_outcome_prediction R_affected_subgoal R_failure_localization R_minimal_repair R_unaffected_preservation</p> <p>So the dataset can train agents to understand:</p> <p>why a plan works what would break how to repair it which parts should remain unchanged</p> <p>This is much richer than sparse final reward.</p> <h3 id="8-core-motivation-in-one-paragraph">8. Core motivation in one paragraph</h3> <p>Current long-horizon agent benchmarks mainly evaluate whether an agent can complete a task, but final success does not reveal whether the agent understands the causal dependencies inside its own trajectory. Real agents must operate under changing observations, imperfect tool outputs, hidden environment states, and failed subgoals. Therefore, we need a benchmark that tests whether models can reason counterfactually over agent trajectories: if one action, observation, tool result, plan step, or subgoal were changed, what downstream states, decisions, and outcomes would change? This capability is essential for robust planning, failure diagnosis, and minimal plan repair.</p> <h2 id="relationship-to-existing-work">Relationship to existing work</h2> <h3 id="hierarchical-rl">Hierarchical RL</h3> <p>Traditional hierarchical reinforcement learning already studies temporal abstraction:</p> <ul> <li>Options framework</li> <li>MAXQ decomposition</li> <li>Feudal RL</li> <li>Subgoal discovery</li> <li>Skill learning</li> </ul> <p>These methods decompose long-horizon tasks into higher-level policies and lower-level actions. However, classical HRL usually assumes environment states/actions, not natural-language plans.</p> <h3 id="llm-agent-planning">LLM agent planning</h3> <p>Recent LLM-agent work uses explicit planning and decomposition:</p> <ul> <li>Plan-and-Act style frameworks separate a planner from an executor.</li> <li>Web-agent methods use high-level plans to guide low-level browser actions.</li> <li>TravelPlanner, WebArena, WebVoyager, DeepPlanning, and related benchmarks test long-horizon constrained planning.</li> <li>ReAct / Reflexion / Tree-of-Thought / plan-execute-replan methods use reasoning traces and feedback loops.</li> </ul> <p>But many such systems are prompt- or inference-time frameworks, not fully trained with hierarchical reward signals.</p> <h3 id="planning-benchmarks">Planning benchmarks</h3> <p>Relevant benchmarks include:</p> <ul> <li><strong>PlanBench</strong>: formal planning and reasoning about actions/change.</li> <li><strong>TravelPlanner</strong>: real-world travel planning with constraints and tools.</li> <li><strong>DeepPlanning</strong>: long-horizon planning with travel and shopping tasks.</li> <li><strong>WebArena / WebVoyager</strong>: web-navigation tasks requiring many steps.</li> <li><strong>Robotouille</strong>: asynchronous planning benchmark.</li> <li><strong>Flex-TravelPlanner</strong>: flexible planning with changing constraints.</li> <li><strong>REALM-Bench</strong>: real-world planning scenarios and adaptation to disruptions.</li> <li><strong>HeroBench</strong>: long-horizon hierarchical planning in an RPG-like world.</li> </ul> <p>These benchmarks are relevant because they can expose failures in decomposition, execution, constraint satisfaction, and replanning.</p> <h2 id="existing-design-patterns-close-to-your-idea">Existing design patterns close to your idea</h2> <h3 id="1-plan-and-execute-agents">1. Plan-and-execute agents</h3> <p>A model first generates a high-level plan, then another module executes it. The plan provides structure, but the system may still lack explicit reward for whether the plan hierarchy itself is correct.</p> <h3 id="2-replan--revise-loops">2. Replan / revise loops</h3> <p>Agents can revise the plan after observing failure. This is close to your idea, but many works treat revision as an inference-time heuristic rather than a trainable ability with explicit revision rewards.</p> <h3 id="3-verifier-guided-planning">3. Verifier-guided planning</h3> <p>A verifier checks whether a plan or step satisfies constraints. This can provide intermediate reward:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>reward = final_success + constraint_satisfaction + verifier_score
</code></pre></div></div> <h3 id="4-process-reward-models">4. Process reward models</h3> <p>Instead of only rewarding final answer correctness, a process reward model scores intermediate reasoning steps. This idea can be adapted from math/code reasoning to long-horizon agent planning.</p> <h3 id="5-hierarchical-goalsubgoal-rewards">5. Hierarchical goal/subgoal rewards</h3> <p>A reward can be assigned at multiple levels:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R_total = R_final + R_plan + R_subgoal + R_step + R_revision
</code></pre></div></div> <p>This is the direct version of your proposal.</p> <h2 id="proposed-method">Proposed method</h2> <h3 id="1-overall-data-format">1. Overall data format</h3> <p>For each benchmark trajectory, convert it into examples like this:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"benchmark"</span><span class="p">:</span><span class="w"> </span><span class="s2">"ScienceWorld"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"task_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"goal"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Determine whether unknown substance X conducts electricity."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"factual_trajectory"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"t"</span><span class="p">:</span><span class="w"> </span><span class="mi">0</span><span class="p">,</span><span class="w"> </span><span class="nl">"obs"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w"> </span><span class="nl">"action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"go to kitchen"</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"t"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="nl">"obs"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w"> </span><span class="nl">"action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"pick up wire"</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"t"</span><span class="p">:</span><span class="w"> </span><span class="mi">2</span><span class="p">,</span><span class="w"> </span><span class="nl">"obs"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w"> </span><span class="nl">"action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"connect wire to battery"</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"t"</span><span class="p">:</span><span class="w"> </span><span class="mi">3</span><span class="p">,</span><span class="w"> </span><span class="nl">"obs"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w"> </span><span class="nl">"action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"test substance X"</span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"observable_state_t"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"hidden_state_t"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"substance_X_property"</span><span class="p">:</span><span class="w"> </span><span class="s2">"conductive"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"battery_charge"</span><span class="p">:</span><span class="w"> </span><span class="s2">"low"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"wire_connected"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"intervention"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"hidden_state_change"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"target"</span><span class="p">:</span><span class="w"> </span><span class="s2">"battery_charge"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"from"</span><span class="p">:</span><span class="w"> </span><span class="s2">"low"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"to"</span><span class="p">:</span><span class="w"> </span><span class="s2">"dead"</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"counterfactual_question"</span><span class="p">:</span><span class="w"> </span><span class="s2">"If the battery had been dead at step 3, would the agent still correctly determine whether substance X conducts electricity?"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"expected_reasoning"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="s2">"The test result would become unreliable because the circuit would not work."</span><span class="p">,</span><span class="w">
    </span><span class="s2">"The observed non-conductivity could be caused by the dead battery rather than the material."</span><span class="p">,</span><span class="w">
    </span><span class="s2">"The agent should first test or replace the battery."</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"counterfactual_outcome"</span><span class="p">:</span><span class="w"> </span><span class="s2">"failure_or_uncertain"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"affected_subgoals"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"test conductivity"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"minimal_repair"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"verify battery works"</span><span class="p">,</span><span class="w"> </span><span class="s2">"replace battery"</span><span class="p">,</span><span class="w"> </span><span class="s2">"repeat conductivity test"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"unaffected_subgoals"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"collect wire"</span><span class="p">,</span><span class="w"> </span><span class="s2">"collect substance"</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>This is different from ordinary planning QA because the answer requires reasoning over a latent state variable that affects future observations.</p> <h3 id="2-choose-benchmarks-where-hidden-state-is-recoverable">2. Choose benchmarks where hidden state is recoverable</h3> <p>You want benchmarks where the environment has an explicit state, database, simulator, or verifier. These are easier to convert into counterfactual data.</p> <h4 id="best-candidates">Best candidates</h4> <p>Benchmark Why good for hidden-state counterfactuals ScienceWorld Text simulator with physical/scientific state variables; agents issue text actions and receive observations. It is explicitly designed for interactive grounded scientific reasoning. ALFWorld TextWorld environments aligned with ALFRED embodied tasks; good for object-location, containment, cleanliness, heat/cool, and receptacle hidden states. TravelPlanner Has structured constraints, tools, travel records, and reference plans; good for hidden availability, budget, opening hours, location, route, and feasibility states. WebArena Self-hostable realistic websites, external tools, knowledge bases, and long-horizon web tasks; good for hidden database/product/account/task-management states. PlanBench Symbolic action preconditions/effects; good for exact counterfactual plan validity, though less realistic. BabyAI / MiniGrid Fully controllable symbolic world; good for clean experiments but less realistic.</p> <p>For a first paper, I would choose ScienceWorld + TravelPlanner. ScienceWorld gives simulator-grounded hidden states; TravelPlanner gives realistic natural-language planning and constraints.</p> <h3 id="3-define-hidden-state-formally">3. Define “hidden state” formally</h3> <p>For each trajectory, distinguish:</p> <p>observation o_t: what the agent sees hidden state z_t: true environment/system variables not fully exposed action a_t: what the agent does goal g: task objective constraints c: explicit user/system constraints</p> <p>A normal environment transition is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>z_{t+1}, o_{t+1} = T(z_t, a_t)
</code></pre></div></div> <p>A counterfactual intervention changes one variable:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>do(z_t^k = v')
</code></pre></div></div> <p>Then the task is to predict:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>z'_{t+1:T}, o'_{t+1:T}, success'
</code></pre></div></div> <p>Your benchmark should test whether the model can infer the downstream effect of hidden state changes.</p> <h3 id="4-types-of-counterfactual-hidden-state-interventions">4. Types of counterfactual hidden-state interventions</h3> <p>You should not only ask “what if action changed?” Make the intervention target a hidden state variable that explains future trajectory changes.</p> <h4 id="type-a-hidden-precondition-change">Type A: Hidden precondition change</h4> <p>The agent does the same action, but a hidden precondition is different.</p> <p>Example:</p> <p>Factual: The door is unlocked, so opening it succeeds. Counterfactual: The door was secretly locked. Question: What future step fails, and what should the agent do?</p> <p>Good for: ALFWorld, BabyAI, WebArena.</p> <h4 id="type-b-hidden-objectproperty-change">Type B: Hidden object/property change</h4> <p>An object has a different latent property.</p> <p>Example:</p> <p>Factual: The substance is conductive. Counterfactual: The substance is non-conductive. Question: How would the experiment observation and conclusion change?</p> <p>Good for: ScienceWorld.</p> <h4 id="type-c-hidden-availability-change">Type C: Hidden availability change</h4> <p>A resource is unavailable, but the agent only discovers this later.</p> <p>Example:</p> <p>Factual: Ferry to Cozumel is available on Day 4. Counterfactual: Ferry is unavailable on Day 4. Question: Which parts of the itinerary become invalid?</p> <p>Good for: TravelPlanner.</p> <h4 id="type-d-hidden-database-change">Type D: Hidden database change</h4> <p>A website backend state changes.</p> <p>Example:</p> <p>Factual: Product X is in stock. Counterfactual: Product X is out of stock. Question: Which click/purchase action would fail, and what alternative path should be taken?</p> <p>Good for: WebArena.</p> <h4 id="type-e-hidden-causal-variable-change">Type E: Hidden causal variable change</h4> <p>A variable affects future observations indirectly.</p> <p>Example:</p> <p>Factual: Plant receives sunlight, later grows. Counterfactual: Plant was kept in darkness. Question: Which later observation changes and what conclusion should be revised?</p> <p>Good for: ScienceWorld.</p> <h4 id="type-f-hidden-agent-memory--information-state-change">Type F: Hidden agent-memory / information state change</h4> <p>The world is the same, but the agent lacks a key observation.</p> <p>Example:</p> <p>Factual: Agent checked the hotel cancellation policy. Counterfactual: Agent skipped that check. Question: What uncertainty remains? Should the final plan be accepted?</p> <p>Good for: WebArena, TravelPlanner.</p> <p>This is important because long-horizon agents often fail not only from bad actions, but from missing information.</p> <h3 id="5-extraction-pipeline">5. Extraction pipeline</h3> <h4 id="step-1-collect-factual-successful-and-failed-trajectories">Step 1: Collect factual successful and failed trajectories</h4> <p>For each benchmark, run agents or use existing trajectories.</p> <p>Store:</p> <p>goal initial observation actions observations final outcome environment state snapshots if available tool/database outputs subgoal annotations if available</p> <p>For simulator benchmarks like ScienceWorld and ALFWorld, you can log state after every step because the environment is executable. ScienceWorld is an interactive text environment where text commands change the world and return textual observations, making state/action logging natural.</p> <p>For WebArena, because it is self-hostable and built from functional websites with reproducible task setups, you can log DOM state, database state, page observations, and tool outputs during each trajectory.</p> <p>For TravelPlanner, log structured travel plans, tool calls, returned records, and constraint satisfaction checks. TravelPlanner includes tools and large travel records, and the agent must formulate plans covering transportation, meals, attractions, and accommodation under constraints.</p> <h4 id="step-2-parse-the-trajectory-into-a-hierarchy">Step 2: Parse the trajectory into a hierarchy</h4> <p>Convert each trajectory into:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Goal
 ├── High-level plan
 ├── Subgoals
 │    ├── information-gathering actions
 │    ├── environment-changing actions
 │    └── verification actions
 └── final answer / completion action
</code></pre></div></div> <p>Example for TravelPlanner:</p> <p>Goal: 5-day Cancun trip</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Subgoals:
S1: choose hotel
S2: schedule Chichen Itza + cenote
S3: schedule Cozumel
S4: schedule Isla Mujeres
S5: schedule Xplor
S6: verify budget and timing
</code></pre></div></div> <p>Example for ScienceWorld:</p> <p>Goal: Determine if unknown material conducts electricity</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Subgoals:
S1: collect circuit components
S2: build circuit
S3: test known conductive object
S4: test unknown object
S5: infer property
</code></pre></div></div> <p>This hierarchy matters because your counterfactual questions should ask which subgoal is affected by the hidden-state change.</p> <h4 id="step-3-identify-hidden-state-variables">Step 3: Identify hidden state variables</h4> <p>For each benchmark, define a hidden-state schema.</p> <h4 id="scienceworld-hidden-state-schema">ScienceWorld hidden state schema</h4> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"object_location"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"object_temperature"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"object_state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"solid/liquid/gas"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"material_property"</span><span class="p">:</span><span class="w"> </span><span class="s2">"conductive/nonconductive/magnetic/soluble"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"container_contents"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"device_state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"on/off/broken/charged"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"experiment_condition"</span><span class="p">:</span><span class="w"> </span><span class="s2">"controlled/uncontrolled"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h4 id="alfworld-hidden-state-schema">ALFWorld hidden state schema</h4> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"object_location"</span><span class="p">:</span><span class="w"> </span><span class="s2">"drawer/table/fridge/sink"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"container_open"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"object_clean"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"object_hot"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"object_cool"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"object_sliced"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"agent_inventory"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h4 id="travelplanner-hidden-state-schema">TravelPlanner hidden state schema</h4> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"flight_available"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"hotel_available"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"attraction_open"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"transport_time"</span><span class="p">:</span><span class="w"> </span><span class="mi">90</span><span class="p">,</span><span class="w">
  </span><span class="nl">"hidden_fee"</span><span class="p">:</span><span class="w"> </span><span class="mi">30</span><span class="p">,</span><span class="w">
  </span><span class="nl">"weather_feasible"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"distance_feasible"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"reservation_required"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h4 id="webarena-hidden-state-schema">WebArena hidden state schema</h4> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"product_stock"</span><span class="p">:</span><span class="w"> </span><span class="s2">"in_stock/out_of_stock"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"product_compatibility"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"cart_contents"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"user_permission"</span><span class="p">:</span><span class="w"> </span><span class="s2">"admin/member/guest"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"issue_status"</span><span class="p">:</span><span class="w"> </span><span class="s2">"open/closed"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"form_validation_rule"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"backend_record"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The hidden-state schema is the core of the dataset. It lets you generate controlled counterfactual interventions.</p> <h4 id="step-4-generate-counterfactual-interventions">Step 4: Generate counterfactual interventions</h4> <p>For each trajectory, choose one time step t and one hidden variable z_t^k.</p> <p>Then create an intervention:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>do(z_t^k = v')
</code></pre></div></div> <p>You want interventions that are:</p> <p>Plausible The changed hidden state could realistically occur. Causally relevant It affects later observations, success, or required repair. Nontrivial The answer cannot be solved by keyword matching. Local but downstream The intervention happens at one point, but the effect appears later.</p> <h4 id="intervention-generation-algorithm">Intervention generation algorithm</h4> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">trajectory</span> <span class="ow">in</span> <span class="n">trajectories</span><span class="p">:</span>
    <span class="n">state_trace</span> <span class="o">=</span> <span class="n">trajectory</span><span class="p">.</span><span class="n">hidden_states</span>
    <span class="n">subgoals</span> <span class="o">=</span> <span class="nf">parse_subgoals</span><span class="p">(</span><span class="n">trajectory</span><span class="p">)</span>
</code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="n">candidate_steps</span><span class="p">:</span>
        <span class="k">for</span> <span class="n">variable</span> <span class="ow">in</span> <span class="nf">mutable_hidden_vars</span><span class="p">(</span><span class="n">state_trace</span><span class="p">[</span><span class="n">t</span><span class="p">]):</span>
            <span class="n">alt_values</span> <span class="o">=</span> <span class="nf">plausible_alternatives</span><span class="p">(</span><span class="n">variable</span><span class="p">)</span>
</code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>            <span class="k">for</span> <span class="n">v_alt</span> <span class="ow">in</span> <span class="n">alt_values</span><span class="p">:</span>
                <span class="n">cf_env</span> <span class="o">=</span> <span class="nf">clone_env_at_step</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>
                <span class="n">cf_env</span><span class="p">.</span><span class="nf">set_hidden_state</span><span class="p">(</span><span class="n">variable</span><span class="p">,</span> <span class="n">v_alt</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>            cf_rollout = replay_or_resimulate(cf_env, trajectory.actions[t:])

            if cf_rollout.outcome != trajectory.outcome:
                save_counterfactual_example(...)
</code></pre></div></div> <p>There are two ways to get the counterfactual outcome:</p> <h4 id="option-1-replay-same-future-actions">Option 1: Replay same future actions</h4> <p>You keep future actions fixed and ask:</p> <p>If the world had changed but the agent followed the same trajectory, what would happen?</p> <p>This tests outcome prediction.</p> <h4 id="option-2-allow-optimal-repair">Option 2: Allow optimal repair</h4> <p>You ask:</p> <p>Given this hidden change, what should the agent revise?</p> <p>This tests planning and revision.</p> <p>You should include both.</p> <h3 id="6-how-to-obtain-labels">6. How to obtain labels</h3> <h4 id="label-type-1-simulated-counterfactual-outcome">Label type 1: Simulated counterfactual outcome</h4> <p>For executable environments, directly run the simulator after the intervention.</p> <p>Works for:</p> <p>ScienceWorld ALFWorld BabyAI MiniGrid some WebArena states if you control the backend</p> <p>Label:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"counterfactual_success"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"changed_observations"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"failed_action"</span><span class="p">:</span><span class="w"> </span><span class="s2">"test substance"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"failure_reason"</span><span class="p">:</span><span class="w"> </span><span class="s2">"battery dead"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h4 id="label-type-2-constraint-verifier-outcome">Label type 2: Constraint-verifier outcome</h4> <p>For planning benchmarks like TravelPlanner, use a verifier.</p> <p>TravelPlanner already evaluates plans by constraint satisfaction, and its tasks involve multiple constraint types including hard constraints, commonsense constraints, and environmental constraints.</p> <p>Example verifier:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">verify_travel_plan</span><span class="p">(</span><span class="n">plan</span><span class="p">,</span> <span class="n">hidden_state</span><span class="p">):</span>
    <span class="n">checks</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">hotel_available</span><span class="sh">"</span><span class="p">:</span> <span class="nf">check_hotel_availability</span><span class="p">(</span><span class="n">plan</span><span class="p">,</span> <span class="n">hidden_state</span><span class="p">),</span>
        <span class="sh">"</span><span class="s">budget</span><span class="sh">"</span><span class="p">:</span> <span class="nf">check_budget</span><span class="p">(</span><span class="n">plan</span><span class="p">),</span>
        <span class="sh">"</span><span class="s">route_feasible</span><span class="sh">"</span><span class="p">:</span> <span class="nf">check_transport</span><span class="p">(</span><span class="n">plan</span><span class="p">,</span> <span class="n">hidden_state</span><span class="p">),</span>
        <span class="sh">"</span><span class="s">attraction_open</span><span class="sh">"</span><span class="p">:</span> <span class="nf">check_opening_hours</span><span class="p">(</span><span class="n">plan</span><span class="p">,</span> <span class="n">hidden_state</span><span class="p">),</span>
        <span class="sh">"</span><span class="s">meal_feasible</span><span class="sh">"</span><span class="p">:</span> <span class="nf">check_meals</span><span class="p">(</span><span class="n">plan</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">checks</span>
</code></pre></div></div> <p>Label:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"counterfactual_success"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"violated_constraints"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"ferry_unavailable"</span><span class="p">,</span><span class="w"> </span><span class="s2">"arrival_too_late"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"affected_days"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"Day 4"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"affected_subgoals"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"visit Cozumel"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"minimal_repair"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"replace Cozumel with Isla Mujeres"</span><span class="p">,</span><span class="w"> </span><span class="s2">"move Cozumel to Day 2 if ferry available"</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h4 id="label-type-3-llm-assisted-but-verifier-checked-repair">Label type 3: LLM-assisted but verifier-checked repair</h4> <p>For minimal repairs, exact simulation may not provide a repair. Use an LLM to propose candidate repairs, then use a verifier to check them.</p> <p>Pipeline:</p> <p>counterfactual failed plan → generate 5 repairs → run verifier → choose valid repair with smallest edit distance from factual plan</p> <p>Minimality score:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>minimality = valid_repair - number_of_unnecessary_changes
</code></pre></div></div> <p>This is useful for TravelPlanner and WebArena.</p> <h3 id="7-counterfactual-question-templates">7. Counterfactual question templates</h3> <p>Your dataset should contain multiple question types, not just one.</p> <p>Type 1: Outcome prediction Given the trajectory below, suppose that hidden state H had been different at step t. Would the task still succeed? Explain why.</p> <p>Example:</p> <p>Suppose the hotel selected on Day 3 was actually unavailable, although the agent had not checked availability yet. Would the final travel plan still satisfy the user request? Type 2: Future observation prediction What observation would the agent receive at step t+1 under this counterfactual?</p> <p>Example:</p> <p>If the battery were dead before the conductivity test, what would the agent observe when connecting the circuit? Type 3: Affected-subgoal localization Which subgoals are affected by this hidden-state change?</p> <p>Example:</p> <p>If the Cozumel ferry is unavailable on Day 4, which parts of the itinerary become invalid? Type 4: Minimal repair What is the smallest change to the plan that restores success?</p> <p>Example:</p> <p>The ferry is unavailable on Day 4. Modify the itinerary minimally while preserving all valid days. Type 5: Contrast factual vs. counterfactual Compare the factual and counterfactual trajectories. What changes and what remains the same?</p> <p>Example:</p> <p>In the factual trajectory, the agent successfully buys product X. In the counterfactual, product X is incompatible. Which future actions should change? Type 6: Hidden-state inference Given the changed observation, what hidden state most likely changed?</p> <p>Example:</p> <p>The agent expected the door to open, but it did not. Which hidden state variable is most likely different?</p> <p>This is the inverse problem and is very important for world modeling.</p> <p>Type 7: Counterfactual plan-step evaluation If the agent had skipped step k, would the task still succeed?</p> <p>Example:</p> <p>If the agent had not verified the opening hours of the ruins, would the final plan be reliable?</p> <p>This tests whether the model understands why an information-gathering step matters.</p> <h3 id="8-benchmark-specific-conversion-recipes">8. Benchmark-specific conversion recipes</h3> <h4 id="a-scienceworld--counterfactual-hidden-state-data">A. ScienceWorld → counterfactual hidden-state data</h4> <p>ScienceWorld is probably the cleanest source because it is an interactive text environment for scientific reasoning, with commands that change the world state and observations.</p> <h4 id="extract">Extract</h4> <p>For each episode:</p> <p>task goal action trace observation trace world state trace object properties device states experiment result final success</p> <h4 id="hidden-state-interventions">Hidden-state interventions</h4> <p>Examples:</p> <p>change material property: conductive → nonconductive change device state: battery charged → dead change container contents: water → saltwater change temperature: hot → cold change object location: beaker on table → beaker in cabinet change contamination state: clean → contaminated</p> <h4 id="counterfactual-questions">Counterfactual questions</h4> <p>If the unknown material were nonconductive instead of conductive, what result would the conductivity test produce? If the battery had been dead, would observing no light prove the material is nonconductive? If the beaker already contained salt, would the conclusion about solubility still be valid?</p> <h4 id="why-good">Why good</h4> <p>This directly tests hidden-state causal reasoning, not just plan execution.</p> <h4 id="b-alfworld--counterfactual-hidden-state-data">B. ALFWorld → counterfactual hidden-state data</h4> <p>ALFWorld uses interactive TextWorld environments aligned with embodied ALFRED tasks, so it is good for household object-state reasoning.</p> <h4 id="extract-1">Extract</h4> <p>goal instruction rooms objects containers agent inventory object states action trace observation trace success/failure</p> <h4 id="hidden-state-interventions-1">Hidden-state interventions</h4> <p>object location changed container secretly closed/open object already dirty/clean object already hot/cold receptacle unavailable object inside another container</p> <h4 id="counterfactual-questions-1">Counterfactual questions</h4> <p>The factual trajectory succeeds because the apple is on the table. If the apple were inside the closed fridge instead, which action would first fail? If the mug were already clean, which subgoal could be skipped? If the microwave were broken, would the same plan still heat the potato?</p> <h4 id="why-good-1">Why good</h4> <p>It supports clear subgoal labels:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>find object → pick object → change object state → place object
</code></pre></div></div> <h4 id="c-travelplanner--counterfactual-hidden-state-data">C. TravelPlanner → counterfactual hidden-state data</h4> <p>TravelPlanner is highly aligned with your hierarchical planning idea because agents must plan transportation, meals, attractions, and accommodation while satisfying multiple constraints.</p> <h4 id="extract-2">Extract</h4> <p>user request destination date range budget constraints tool calls returned records reference plan final predicted plan constraint-check results</p> <h4 id="hidden-state-interventions-2">Hidden-state interventions</h4> <p>hotel unavailable restaurant closed attraction closed transport delayed hidden fee added distance/time changed weather makes activity infeasible reservation required flight arrival delayed</p> <h4 id="counterfactual-questions-2">Counterfactual questions</h4> <p>If the selected hotel were unavailable on Night 3, which parts of the plan become invalid? If the flight arrived 3 hours later, would the Day 1 attraction schedule still be feasible? If the cenote tour required a reservation that the agent did not make, what minimal repair is needed? If the restaurant is closed on Tuesday, does this only affect the meal subgoal or also the transportation schedule? Hidden-state examples</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"hidden_state"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"hotel_A_availability"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
    </span><span class="nl">"ferry_Cozumel_day4"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
    </span><span class="nl">"xplor_opening_hours"</span><span class="p">:</span><span class="w"> </span><span class="s2">"9am-5pm"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"tour_requires_reservation"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
    </span><span class="nl">"actual_transport_time"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2.5h"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h4 id="why-good-2">Why good</h4> <p>TravelPlanner is ideal for evaluating:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>goal → days → subgoals → constraints → hidden feasibility → minimal repair
</code></pre></div></div> <p>This is probably the best non-simulator starting point for your paper.</p> <h4 id="d-webarena--counterfactual-hidden-state-data">D. WebArena → counterfactual hidden-state data</h4> <p>WebArena is a self-hostable web environment with realistic websites, external tools, and knowledge resources. It includes tasks across domains such as e-commerce, forums, software development, and content management.</p> <h4 id="extract-3">Extract</h4> <p>task instruction browser actions DOM observations backend database states forms submitted cart/account/project states final success</p> <h4 id="hidden-state-interventions-3">Hidden-state interventions</h4> <p>product out of stock user lacks permission form validation rule changed forum post deleted issue already closed price changed shipping address invalid coupon expired search index stale</p> <h4 id="counterfactual-questions-3">Counterfactual questions</h4> <p>If the product had been out of stock after the agent added it to the cart, which later action would fail? If the user did not have admin permission, would the same website action succeed? If the search result was stale, what additional verification step should the agent take?</p> <h4 id="why-good-3">Why good</h4> <p>This is realistic but harder, because hidden state may be distributed across backend database, DOM, and user account state.</p> <h3 id="9-constructing-hidden-state-reasoning-labels">9. Constructing “hidden-state reasoning” labels</h3> <p>For each counterfactual example, annotate the answer in structured form.</p> <h4 id="required-labels">Required labels</h4> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"outcome"</span><span class="p">:</span><span class="w"> </span><span class="s2">"success/failure/uncertain"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"first_divergent_step"</span><span class="p">:</span><span class="w"> </span><span class="mi">5</span><span class="p">,</span><span class="w">
  </span><span class="nl">"changed_hidden_state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"battery_charge"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"changed_observation"</span><span class="p">:</span><span class="w"> </span><span class="s2">"light bulb does not turn on"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"affected_subgoals"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"test conductivity"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"unaffected_subgoals"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"collect materials"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"failure_reason"</span><span class="p">:</span><span class="w"> </span><span class="s2">"test apparatus invalid"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"minimal_repair"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"replace battery"</span><span class="p">,</span><span class="w"> </span><span class="s2">"repeat test"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"reasoning_type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"hidden_precondition"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h4 id="optional-labels">Optional labels</h4> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"causal_chain"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="s2">"battery dead"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"circuit cannot close"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"bulb does not light"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"observation is ambiguous"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"cannot infer material conductivity"</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"counterfactual_plan"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"state_diff"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"battery_charge"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"charged"</span><span class="p">,</span><span class="w"> </span><span class="s2">"dead"</span><span class="p">]</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"observation_diff"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">],</span><span class="w">
  </span><span class="nl">"constraint_diff"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="err">...</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The causal chain is especially useful for training.</p> <h3 id="10-automatic-generation-algorithm">10. Automatic generation algorithm</h3> <p>A practical version:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">generate_cf_examples</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">task</span><span class="p">,</span> <span class="n">policy_or_trace</span><span class="p">):</span>
    <span class="n">factual</span> <span class="o">=</span> <span class="nf">run_or_load_trajectory</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">task</span><span class="p">,</span> <span class="n">policy_or_trace</span><span class="p">)</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## 1. Save factual state/action/obs trace
trace = factual.trace
state_trace = factual.hidden_states

examples = []

## 2. Select candidate time steps ```python
for t in select_relevant_steps(trace):
    z_t = state_trace[t] ```

    ## 3. Select mutable hidden variables ```python
    for var in candidate_hidden_vars(z_t):
        for alt in plausible_values(var, z_t[var]): ```

            ## 4. Create counterfactual environment
            cf_env = clone_env_at_state(env, t)
            cf_env.set_hidden_state(var, alt)

            ## 5. Replay same future actions
            cf_replay = replay_actions(
                cf_env,
                actions=trace.actions[t:]
            )

            ## 6. Compare factual vs counterfactual
            diff = compare_trajectories(factual, cf_replay)

            if is_interesting(diff):
                ## 7. Generate question/answer
                example = build_cf_qa(
                    factual=factual,
                    counterfactual=cf_replay,
                    intervention=(var, alt),
                    diff=diff
                )
                examples.append(example)

return examples
</code></pre></div></div> <p>is_interesting(diff) should filter out changes that do not affect anything meaningful.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">is_interesting</span><span class="p">(</span><span class="n">diff</span><span class="p">):</span>
    <span class="nf">return </span><span class="p">(</span>
        <span class="n">diff</span><span class="p">.</span><span class="n">final_success_changed</span>
        <span class="ow">or</span> <span class="n">diff</span><span class="p">.</span><span class="n">first_divergent_observation</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span>
        <span class="ow">or</span> <span class="nf">len</span><span class="p">(</span><span class="n">diff</span><span class="p">.</span><span class="n">affected_subgoals</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">0</span>
        <span class="ow">or</span> <span class="n">diff</span><span class="p">.</span><span class="n">requires_repair</span>
    <span class="p">)</span>
</code></pre></div></div> <h3 id="11-how-to-make-questions-genuinely-require-hidden-state-reasoning">11. How to make questions genuinely require hidden-state reasoning</h3> <p>A bad counterfactual question is too obvious:</p> <p>If the hotel is unavailable, can you stay there?</p> <p>A good hidden-state reasoning question requires delayed consequences:</p> <p>The agent selected Hotel A because it is near the ferry terminal and within budget. Suppose Hotel A was actually unavailable on Night 3, but this is only discovered at checkout. Which later parts of the itinerary are affected, and what is the minimal repair?</p> <p>Why better?</p> <p>The model must reason about hidden availability. It must identify downstream dependencies. It must preserve unaffected plan parts. It must revise minimally.</p> <h4 id="add-distractors">Add distractors</h4> <p>Include irrelevant hidden-state changes:</p> <p>Hotel lobby has no gym.</p> <p>This should not affect a sightseeing itinerary unless the user required a gym.</p> <h4 id="add-delayed-effects">Add delayed effects</h4> <p>The changed state should affect something 3–8 steps later.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Flight delay → missed check-in → hotel cancellation → no lodging → next day schedule affected
</code></pre></div></div> <h4 id="add-partial-observability">Add partial observability</h4> <p>The model should know that the agent cannot conclude something yet.</p> <p>If the battery is dead, a failed conductivity test is ambiguous.</p> <p>This tests epistemic reasoning, not just outcome prediction.</p> <h3 id="12-traintest-splits">12. Train/test splits</h3> <p>To show the dataset is not just pattern matching, use splits that test generalization.</p> <h4 id="split-by-hidden-state-type">Split by hidden-state type</h4> <p>Train on:</p> <p>availability changes, object-location changes</p> <p>Test on:</p> <p>device-state changes, permission changes</p> <h4 id="split-by-benchmark-domain">Split by benchmark domain</h4> <p>Train on:</p> <p>TravelPlanner</p> <p>Test on:</p> <p>WebArena shopping</p> <h4 id="split-by-intervention-distance">Split by intervention distance</h4> <p>Train on short-distance counterfactuals:</p> <p>intervention affects next 1–2 steps</p> <p>Test on long-distance counterfactuals:</p> <p>intervention affects step t+5 or later</p> <h4 id="split-by-hierarchy-level">Split by hierarchy level</h4> <p>Train on action-level counterfactuals.</p> <p>Test on:</p> <p>subgoal-level or plan-level counterfactuals</p> <p>This directly tests your hierarchical planning hypothesis.</p> <h3 id="13-evaluation-metrics">13. Evaluation metrics</h3> <p>Use structured metrics instead of only answer accuracy.</p> <h4 id="outcome-accuracy">Outcome accuracy</h4> <p>Did the model correctly predict success/failure/uncertain?</p> <h4 id="first-divergence-accuracy">First divergence accuracy</h4> <p>Did it identify the first step where the counterfactual trajectory differs?</p> <h4 id="affected-subgoal-f1">Affected subgoal F1</h4> <p>F1 between predicted affected subgoals and gold affected subgoals.</p> <h4 id="hidden-state-causal-variable-accuracy">Hidden-state causal variable accuracy</h4> <p>Did it identify which hidden variable caused the change?</p> <h4 id="minimal-repair-validity">Minimal repair validity</h4> <p>Does the proposed repair satisfy constraints?</p> <h4 id="minimal-repair-edit-distance">Minimal repair edit distance</h4> <p>How much unnecessary change did the model make?</p> <h4 id="unaffected-subgoal-preservation">Unaffected-subgoal preservation</h4> <p>Did the model preserve parts of the plan that did not need changing?</p> <h4 id="causal-chain-score">Causal chain score</h4> <p>Evaluate whether the explanation includes the required causal links:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hidden state → changed observation → failed subgoal → final outcome
</code></pre></div></div> <p>This is crucial for your paper because you want reasoning over hidden states, not just final answer prediction.</p> <h3 id="14-dataset-variants">14. Dataset variants</h3> <p>You can create three versions with increasing difficulty.</p> <h4 id="level-1-direct-hidden-state-effect">Level 1: Direct hidden-state effect</h4> <p>Hidden state changes next observation immediately.</p> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Battery dead → bulb does not light.
</code></pre></div></div> <h4 id="level-2-delayed-hidden-state-effect">Level 2: Delayed hidden-state effect</h4> <p>Hidden state affects a future subgoal several steps later.</p> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hotel unavailable → check-in fails later → next day transport plan invalid.
</code></pre></div></div> <h4 id="level-3-branching-counterfactual-repair">Level 3: Branching counterfactual repair</h4> <p>Hidden state causes failure, and model must propose a minimal valid alternative trajectory.</p> <p>Example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Cozumel ferry unavailable → choose whether to swap days, replace activity, or change hotel.
</code></pre></div></div> <p>Level 3 is the most publishable and best aligned with hierarchical rewards.</p> <h3 id="15-how-this-becomes-training-data">15. How this becomes training data</h3> <p>Each example can be used in three training formats.</p> <h4 id="format-a-supervised-reasoning">Format A: Supervised reasoning</h4> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input:
factual trajectory + hidden-state intervention + question
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Output:
causal explanation + outcome + repair
</code></pre></div></div> <h4 id="format-b-preference-data">Format B: Preference data</h4> <p>Generate two answers:</p> <p>A: correct counterfactual causal chain B: shallow answer that ignores hidden state</p> <p>Train with DPO/RLAIF-style preference optimization.</p> <h4 id="format-c-rl-reward">Format C: RL reward</h4> <p>Reward components:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R = R_outcome
  + R_hidden_variable
  + R_affected_subgoal
  + R_causal_chain
  + R_repair_validity
  + R_minimality
</code></pre></div></div> <p>This directly connects to your hierarchical reward idea.</p> <h3 id="16-concrete-first-implementation-plan">16. Concrete first implementation plan</h3> <p>I would start with ScienceWorld + TravelPlanner.</p> <h4 id="phase-1-scienceworld-prototype">Phase 1: ScienceWorld prototype</h4> <h4 id="why-first">Why first?</h4> <p>It is executable. Hidden states are clear. Counterfactual outcomes can be simulated. Scientific tasks naturally involve hidden causal variables.</p> <p>Build 1,000–5,000 examples:</p> <p>conductivity melting/freezing solubility magnetism plant growth state of matter</p> <p>Each example:</p> <p>factual trajectory hidden-state intervention future observation prediction outcome prediction minimal repair</p> <h4 id="phase-2-travelplanner-realistic-extension">Phase 2: TravelPlanner realistic extension</h4> <p>Build 500–2,000 examples:</p> <p>hotel availability attraction opening hours transport delays hidden cost weather infeasibility reservation requirement</p> <p>Use verifier-based labels.</p> <h4 id="phase-3-evaluation">Phase 3: Evaluation</h4> <p>Compare:</p> <p>GPT-style LLM open-source LLM ReAct-style agent plan-and-solve agent agent with explicit state tracker agent trained on your counterfactual data</p> <p>Evaluate:</p> <p>outcome accuracy affected-subgoal F1 minimal-repair validity long-distance dependency accuracy</p> <h3 id="17-paper-contribution-framing">17. Paper contribution framing</h3> <p>Your method can be framed as:</p> <p>We convert existing long-horizon agent benchmarks into counterfactual hidden-state reasoning datasets by intervening on latent environment variables, simulating or verifying the resulting alternative trajectories, and asking models to predict affected observations, subgoals, outcomes, and minimal repairs.</p> <p>This is stronger than just asking “what if?” because the dataset requires:</p> <p>hidden system state reasoning</p> <ul> <li>long-horizon trajectory prediction</li> <li>hierarchical subgoal localization</li> <li>minimal plan repair</li> </ul> <p>That combination is the innovative part.</p>]]></content><author><name></name></author><category term="Idea"/><category term="Agent"/><summary type="html"><![CDATA[Idea]]></summary></entry><entry><title type="html">Compositional Generalization in Diffusion Models</title><link href="https://jefferyy-peng.github.io/blog/2026/diffusion-compositional-generalization/" rel="alternate" type="text/html" title="Compositional Generalization in Diffusion Models"/><published>2026-01-12T16:40:16+00:00</published><updated>2026-01-12T16:40:16+00:00</updated><id>https://jefferyy-peng.github.io/blog/2026/diffusion-compositional-generalization</id><content type="html" xml:base="https://jefferyy-peng.github.io/blog/2026/diffusion-compositional-generalization/"><![CDATA[<h2 id="tldr">TLDR</h2> <ul> <li><strong>Paper 1 (SIM / Swing-by Dynamics)</strong> is a highly abstract wrapper of compositional generalization as <strong>learning an identity mapping</strong> on a structured Gaussian mixture. It ignores system internals (e.g., diffusion sampling, U-Nets) and develops theory for the learning dynamics of resulting simplified problem.</li> <li><strong>Paper 2 (CPC/LCS in Conditional Diffusion)</strong> instead goes into <strong>diffusion models directly</strong>, showing that <strong>local inductive bias</strong> (sparse conditional dependencies in the score) is the mechanism that enables compositional / length generalization.</li> </ul> <hr/> <h1 id="paper-1--swing-by-dynamics-in-concept-learning-and-compositional-generalization-sim-yang-et-al-iclr-2025">Paper 1 — Swing-by Dynamics in Concept Learning and Compositional Generalization (SIM) (Yang et al., ICLR 2025)</h1> <p><em>PDF: <a href="https://arxiv.org/pdf/2410.08309v3.pdf">2410.08309v3</a></em></p> <h2 id="1-motivation-a-theoretical-wrapper-for-concept-space-diffusion-results">1. Motivation: a theoretical wrapper for “concept space” diffusion results</h2> <p>Prior work evaluates a diffusion model by:</p> <ul> <li>mapping conditioning concepts to a vector,</li> <li>using a classifier to map generated images back to concept accuracy vectors,</li> <li>so a “good generator + classifier system” behaves like an <strong>identity mapping</strong> in concept space.</li> </ul> <p>This paper argues:</p> <blockquote> <p>The salient part is the <em>structured organization</em> of concept space, not the diffusion internals.</p> </blockquote> <p>So they introduce a simplified task: <strong>Structured Identity Mapping (SIM)</strong>.</p> <hr/> <h2 id="2-sim-dataset-gaussian-mixture-with-structured-centroids">2. SIM dataset: Gaussian mixture with structured centroids</h2> <p>Let \(d\) be dimension and \(s\le d\) be number of concept classes.</p> <p>Training data consists of \(s\) Gaussian clusters aligned with coordinate axes: \(x_k^{(p)} \sim \mathcal{N}\!\left(\mu_p \mathbf{1}_p,\; \mathrm{diag}(\sigma)^2\right), \quad p\in [s],\; k\in[n].\)</p> <p>Interpretation:</p> <ul> <li>\(\mu_p\) = concept signal strength (cluster mean distance from origin),</li> <li>\(\sigma_p\) = concept diversity (variance along that axis).</li> </ul> <p>The learning problem is identity mapping with MSE: \(L(\theta) = \frac{1}{2sn}\sum_{p=1}^s\sum_{k=1}^n \|f(\theta; x_k^{(p)}) - x_k^{(p)}\|^2.\)</p> <p>Evaluation uses an OOD “composition” point: \(x_b = \sum_{p=1}^s \mu_p \mathbf{1}_p.\)</p> <hr/> <h2 id="3-linearization-loss-in-terms-of-covariance">3. Linearization: loss in terms of covariance</h2> <p>Assume the model is linear in input: \(f(\theta; x)=W_\theta x.\)</p> <p>Then (via trace trick) the loss becomes: \(L(\theta) = \frac{1}{2}\|(W_\theta - I)A^{1/2}\|_F^2,\) where the (population) covariance is diagonal: \(A=\mathbb{E}[xx^\top] = \mathrm{diag}(a), \quad a_p= \begin{cases} \sigma_p^2 + \frac{\mu_p^2}{s}, &amp; p\le s,\\ 0, &amp; p&gt;s. \end{cases}\)</p> <p>Key: learning rates along coordinates are controlled by \(a_p\), hence by \(\mu_p\) and \(\sigma_p\).</p> <hr/> <h2 id="4-one-layer-linear-model-closed-form-dynamics">4. One-layer linear model: closed-form dynamics</h2> <p>For \(f(W;x)=Wx\) under gradient flow, they derive: \(f(W(t),z)_k = \mathbf{1}_{k\le s}\bigl(1-e^{-a_k t}\bigr)z_k + \sum_{i=1}^s e^{-a_i t} w_{k,i}(0) z_i.\)</p> <p>Interpretation:</p> <ul> <li>A “growth” term drives the correct identity mapping,</li> <li>A “noise” term decays with time and initialization scale.</li> </ul> <p>Consequences:</p> <ul> <li>Concepts with larger \(a_k\) converge faster.</li> <li>Since \(a_k\) increases with \(\mu_k\) and \(\sigma_k\), generalization order is governed jointly by <strong>signal strength</strong> and <strong>diversity</strong>.</li> </ul> <p>This reproduces empirical “concept order” phenomena.</p> <p>Limitation:</p> <ul> <li>coordinates evolve independently → no non-monotonic OOD behavior.</li> </ul> <hr/> <h2 id="5-deep-linear-model-symmetric-2-layer-network-and-swing-by-dynamics">5. Deep linear model: symmetric 2-layer network and Swing-by dynamics</h2> <p>They analyze: \(f(U;x)=UU^\top x, \quad W(t)=UU^\top.\)</p> <p>They obtain an evolution equation for Jacobian entries \(w_{i,j}\) decomposed into:</p> <ul> <li>growth term,</li> <li>suppression term,</li> <li>noise term,</li> </ul> <p>which yields <strong>multi-stage dynamics</strong>:</p> <ol> <li>initial growth of many Jacobian entries,</li> <li>one major diagonal entry grows first,</li> <li>it suppresses associated off-diagonal “minor” entries,</li> <li>next major entry grows, and so on.</li> </ol> <p>This staged Jacobian evolution produces an OOD trajectory that:</p> <ul> <li>initially moves toward the OOD composition point,</li> <li>then detours back toward training cluster(s),</li> <li>then later returns to OOD performance.</li> </ul> <p>They call this <strong>Swing-by dynamics</strong> and connect it to a double-descent-like test loss curve (for OOD).</p> <hr/> <h2 id="6-empirical-bridge-back-to-diffusion">6. Empirical bridge back to diffusion</h2> <p>Even though SIM is abstract, they verify in text-conditioned diffusion models that:</p> <ul> <li>OOD concept accuracy can be non-monotonic during training,</li> <li>matching the “Swing-by” mechanism predicted by theory.</li> </ul> <hr/> <h2 id="7-takeaway">7. Takeaway</h2> <p>This paper treats compositional generalization as a <strong>wrapper identity mapping problem</strong>:</p> <ul> <li>it ignores internal generative machinery,</li> <li>and explains sequential concept learning + non-monotonic OOD curves as consequences of optimization dynamics on structured data.</li> </ul> <hr/> <h1 id="paper-2--local-mechanisms-of-compositional-generalization-in-conditional-diffusion-bradley-et-al-2025">Paper 2 — Local Mechanisms of Compositional Generalization in Conditional Diffusion (Bradley et al., 2025)</h1> <p><em>PDF: <a href="https://arxiv.org/pdf/2509.16447v2.pdf">2509.16447v2</a></em></p> <h2 id="1-motivation-why-length-generalization-is-hard">1. Motivation: why length generalization is hard</h2> <p>The paper studies <strong>length generalization</strong> in conditional diffusion: train on scenes with a small number of objects, then test with <strong>more</strong> conditions (e.g., more specified locations) than seen during training.</p> <p>Key observation:</p> <ul> <li>Whether length generalization succeeds depends on whether the model learns a <strong>compositional mechanism</strong> (one object per condition) or a <strong>shortcut mechanism</strong> (condition triggers a typical scene, not additive per-condition behavior).</li> </ul> <h2 id="2-setup-location-conditioned-clevr-experiments">2. Setup: location-conditioned CLEVR experiments</h2> <p>They use CLEVR with location conditioning:</p> <ul> <li><strong>Experiment 1 (success):</strong> conditioner labels <strong>all</strong> object locations.</li> <li><strong>Experiment 2 (failure):</strong> conditioner labels <strong>only one</strong> randomly chosen object location (even if the image has 2–3 objects).</li> <li><strong>Experiment 3 (fix):</strong> enforce an architecture that induces <strong>local conditional score structure</strong>, restoring length generalization.</li> </ul> <p>Crucial point: Even when training does not include multi-location conditioning, length generalization can still happen if the <strong>right inductive bias</strong> forces the model to represent the conditional distribution compositionally.</p> <hr/> <h2 id="3-definitions-score-functions-and-locality">3. Definitions: Score functions and locality</h2> <p>A diffusion model learns the <strong>conditional score</strong>: \(s_t(x \mid c) \;=\; \nabla_x \log p_t(x \mid c).\)</p> <h3 id="31-local-conditional-scores-lcs">3.1 Local Conditional Scores (LCS)</h3> <p>They define <strong>Local Conditional Scores (LCS)</strong> as a sparsity condition on dependencies:</p> <p>At each pixel \(i\), the score depends only on:</p> <ul> <li>a subset of pixels \(N_i\) (often a local neighborhood), and</li> <li>a subset of conditions \(L_i \subseteq J\) (often nearby conditions for location-conditioning).</li> </ul> <p>Informally:</p> <blockquote> <p>The score at pixel \(i\) does not need the entire image nor all conditioners—only a sparse subset.</p> </blockquote> <p>This generalizes “local scores” to conditional settings.</p> <hr/> <h2 id="4-conditional-projective-composition-cpc">4. Conditional Projective Composition (CPC)</h2> <p>They define a strong form of compositionality of the <strong>conditional distribution</strong>.</p> <p>Let \(J\) index the set of conditions \(\{c_j\}_{j \in J}\). Let \(\{M_j\}_{j\in J}\) be <strong>disjoint</strong> pixel subsets, and let \(M_J^c\) denote the remaining pixels.</p> <p>A <strong>CPC</strong> distribution factorizes as: \(p(x \mid c_J) \;=\; p(x_{M_J^c} \mid \varnothing) \prod_{j \in J} p(x_{M_j} \mid c_j).\)</p> <p>Meaning:</p> <ul> <li>Each condition \(c_j\) affects only its own region \(M_j\).</li> <li>Different regions are conditionally independent.</li> <li>A background region \(M_J^c\) may be unconditional.</li> </ul> <p>This structure implies length generalization naturally: adding a new condition adds a new independent factor.</p> <hr/> <h2 id="5-lemma-1-cpc--lcs-exact">5. Lemma 1: CPC ⇒ LCS (exact)</h2> <p>If \(p(x \mid c_J)\) satisfies CPC, then its score is LCS.</p> <p>Sketch: Take logs: \(\log p(x \mid c_J) = \log p(x_{M_J^c}\mid \varnothing) + \sum_{j\in J}\log p(x_{M_j}\mid c_j).\)</p> <p>Differentiate w.r.t. pixel \(i\):</p> <ul> <li>If \(i \in M_j\), then: \(\nabla_{x_i}\log p(x\mid c_J) = \nabla_{x_i}\log p(x_{M_j}\mid c_j),\) so dependence is only on \(x_{M_j}\) and condition \(c_j\).</li> <li>If \(i \in M_J^c\), then the score depends only on the unconditional background term.</li> </ul> <p>Thus compositional distributions have <strong>local conditional</strong> score structure.</p> <hr/> <h2 id="6-relaxation-approximate-cpc--approximate-lcs-and-more-compositional-at-high-noise">6. Relaxation: approximate CPC ⇒ approximate LCS, and “more compositional at high noise”</h2> <p>Real models are not perfectly CPC. The paper relaxes the lemma:</p> <ul> <li>If \(p(x\mid c)\) is <strong>approximately</strong> CPC, then the score is <strong>approximately</strong> LCS.</li> <li>The approximation becomes better at <strong>higher noise</strong> \(t\) (intuitively, noise washes out detailed interactions, leaving large-scale compositional structure).</li> </ul> <p>This supports a diffusion-time decomposition:</p> <ul> <li><strong>High noise:</strong> conditional dependencies dominate; global structure (object count/layout) is established.</li> <li><strong>Low noise:</strong> pixel dependencies dominate; local unconditional denoising fills in details.</li> </ul> <p>This explains why local conditional mechanisms can “set” the compositional structure early.</p> <hr/> <h2 id="7-feature-space-extension-cpclcs-after-an-invertible-transform">7. Feature-space extension: CPC/LCS after an invertible transform</h2> <p>Pixel-space compositionality often fails for prompts like “watercolor cat sushi” (style and content interact everywhere).</p> <p>They propose: Let \(z = A(x)\) be an invertible feature transform. If the pushforward distribution \(A_\#p(z\mid c)\) is CPC, then the <strong>feature-space score</strong> is LCS.</p> <p>This motivates “disentanglement” as CPC/LCS structure in a learned feature space.</p> <h3 id="71-orthogonality-heuristic-for-disentanglement">7.1 Orthogonality heuristic for disentanglement</h3> <p>Define: \(\mu_i := \mathbb{E}_{z\sim A_\#p(\cdot\mid c_i)}[z], \quad \mu_b := \mathbb{E}_{z\sim A_\#p(\cdot\mid \varnothing)}[z], \quad d_i := \mu_i - \mu_b.\)</p> <p>A necessary (not sufficient) condition for CPC is pairwise orthogonality: \(d_i^\top d_j = 0 \quad \forall i\neq j.\)</p> <p>Practically they compute cosine similarity: \(\frac{d_i^\top d_j}{\|d_i\|\|d_j\|},\) where low off-diagonal similarity suggests feature-space disentanglement.</p> <hr/> <h2 id="8-causal-evidence-enforcing-lcs-restores-generalization">8. Causal evidence: enforcing LCS restores generalization</h2> <p>Experiment 3 performs a direct intervention:</p> <ul> <li>keep training distribution like Experiment 2,</li> <li>enforce architectural locality producing LCS-like score dependencies,</li> <li>observe restored length generalization.</li> </ul> <p>Conclusion:</p> <blockquote> <p>The local conditional score structure is not merely correlated with generalization; it is a <strong>causal mechanism</strong>.</p> </blockquote> <hr/> <h2 id="9-takeaway">9. Takeaway</h2> <p>Compositional generalization in conditional diffusion hinges on an <strong>inductive bias</strong>:</p> <ul> <li>representing conditional effects in a sparse / local way (LCS),</li> <li>which corresponds to a compositional factorization of the conditional distribution (CPC).</li> </ul> <hr/> <h1 id="synthesis-how-the-two-papers-complement-each-other">Synthesis: How the two papers complement each other</h1> <ul> <li><strong>Paper 1</strong>: compositionality depends on <em>mechanistic inductive bias in diffusion</em>: local conditional score structure.</li> <li><strong>Paper 2</strong>: compositional phenomena can arise even in a stripped-down identity task: optimization + data geometry create staged learning and Swing-by.</li> </ul> <p>Together:</p> <ul> <li>Paper 2 explains <em>when</em> and <em>in what order</em> concept directions emerge under training dynamics.</li> <li>Paper 1 explains <em>whether</em> a diffusion system will actually realize an additive compositional mechanism, via locality/sparsity constraints in the conditional score.</li> </ul>]]></content><author><name></name></author><category term="Paper-Reading"/><category term="Diffusion"/><category term="Compositional-Generalization"/><summary type="html"><![CDATA[Papers for "Compositional Generalization in Diffusion Models"]]></summary></entry><entry><title type="html">Generalization Through Variance in Diffusion Models</title><link href="https://jefferyy-peng.github.io/blog/2025/diffusion-generalization-variance/" rel="alternate" type="text/html" title="Generalization Through Variance in Diffusion Models"/><published>2025-11-21T16:40:16+00:00</published><updated>2025-11-21T16:40:16+00:00</updated><id>https://jefferyy-peng.github.io/blog/2025/diffusion-generalization-variance</id><content type="html" xml:base="https://jefferyy-peng.github.io/blog/2025/diffusion-generalization-variance/"><![CDATA[<p><strong>TL;DR:</strong> Optimizing locally instead of optimizing a global score function introduces structured noise; this noise induces variance in the learned score, which in turn induces generalization.</p> <h2 id="0-high-level-roadmap-what-the-paper-tries-to-do">0. High-level roadmap (what the paper tries to do)</h2> <p>The paper’s main goal is to analytically characterize the <strong>typical learned sampling distribution</strong> of diffusion models trained with denoising score matching (DSM), and to explain <strong>why they generalize</strong> (place probability mass between training examples) instead of perfectly memorizing.</p> <p>Key steps:</p> <ol> <li>Define forward diffusion and reverse PF-ODE sampling.</li> <li>Compare the “true-score” objective \(J_0\) vs DSM objective \(J_1\).</li> <li>Show the proxy score is <strong>unbiased</strong> but has a structured <strong>covariance</strong>.</li> <li>Treat the trained score estimator \(\hat s_\theta\) as random (because it depends on finite training samples).</li> <li>Write PF-ODE sampling as a <strong>path integral</strong>, so that averaging over training randomness becomes tractable.</li> <li>Perform the ensemble average via cumulants, producing mean term \(M_1\) and covariance term \(M_2\) (the <strong>V-kernel</strong>).</li> <li>Under a Gaussian approximation, show the averaged dynamics are equivalent to an <strong>effective SDE</strong>: generalization happens iff the V-kernel is nonzero.</li> <li>Provide a toy “naive estimator” scheme showing how nonzero V-kernel arises even when using the proxy score directly.</li> </ol> <hr/> <h2 id="1-preliminaries-distributions-conditionals-and-marginals">1. Preliminaries: distributions, conditionals, and marginals</h2> <h3 id="11-data-distribution">1.1 Data distribution</h3> <p>Let \(x_0 \in \mathbb R^D\) denote clean data. The data distribution is \(p_{\text{data}}(x_0)\).</p> <p>A common idealization used in the paper is an empirical distribution over \(M\) examples \(\{\mu_m\}_{m=1}^M\): \(p_{\text{data}}(x_0) = \frac{1}{M}\sum_{m=1}^M \delta(x_0 - \mu_m).\)</p> <p>Here \(\delta(\cdot)\) is the Dirac delta distribution.</p> <hr/> <h2 id="2-forward-diffusion">2. Forward diffusion</h2> <h3 id="21-forward-sde">2.1 Forward SDE</h3> <p>The forward process is an SDE: \(\dot x_t = -\beta_t x_t + G_t \eta_t,\quad t:0\to T.\) Definitions:</p> <ul> <li>\(x_t \in \mathbb R^D\): random variable at time \(t\).</li> <li>\(\dot x_t\): time derivative (informally; in SDE form it corresponds to Ito dynamics).</li> <li>\(\beta_t \ge 0\): scalar drift schedule.</li> <li>\(G_t \in \mathbb R^{D\times K}\): noise injection matrix.</li> <li>\(\eta_t\): standard Gaussian white noise process.</li> </ul> <p>Define diffusion tensor: \(D_t := \frac{G_t G_t^\top}{2}\in \mathbb R^{D\times D}.\)</p> <hr/> <h3 id="22-solve-the-forward-sde-derive-px_tmid-x_0tmathcal-nalpha_t-x_0-s_t">2.2 Solve the forward SDE: derive \(p(x_t\mid x_0,t)=\mathcal N(\alpha_t x_0, S_t)\)</h3> <p>We derive the conditional distribution of \(x_t\) given \(x_0\).</p> <p>Write the SDE in Ito differential form: \(dx_t = -\beta_t x_t dt + G_t dW_t,\) where \(W_t\) is a \(K\)-dim Brownian motion (so \(dW_t\) are Gaussian increments).</p> <h4 id="step-1-integrating-factor">Step 1: integrating factor</h4> <p>Define \(\alpha_t := \exp\left(-\int_0^t \beta_{t'} dt'\right).\) Note: \(\frac{d}{dt}\alpha_t = -\beta_t \alpha_t,\quad \alpha_0=1.\)</p> <p>Consider the scaled process: \(y_t := \alpha_t^{-1} x_t.\)</p> <p>Use Ito (here drift-only scaling works as usual since factor is deterministic): \(dy_t = d(\alpha_t^{-1} x_t) = \alpha_t^{-1} dx_t + x_t d(\alpha_t^{-1}).\) Compute: \(d(\alpha_t^{-1}) = -\alpha_t^{-2} d\alpha_t = -\alpha_t^{-2}(-\beta_t \alpha_t dt) = \beta_t \alpha_t^{-1} dt.\) So: \(dy_t = \alpha_t^{-1}(-\beta_t x_t dt + G_t dW_t) + x_t(\beta_t \alpha_t^{-1} dt) = \alpha_t^{-1} G_t dW_t.\)</p> <h4 id="step-2-integrate">Step 2: integrate</h4> <p>Integrate from 0 to t: \(y_t = y_0 + \int_0^t \alpha_{t'}^{-1} G_{t'} dW_{t'}.\) But \(y_0 = \alpha_0^{-1} x_0 = x_0\). Thus: \(y_t = x_0 + \int_0^t \alpha_{t'}^{-1} G_{t'} dW_{t'}.\)</p> <p>Multiply by \(\alpha_t\): \(x_t = \alpha_t x_0 + \alpha_t\int_0^t \alpha_{t'}^{-1} G_{t'} dW_{t'}.\)</p> <h4 id="step-3-identify-gaussian-distribution">Step 3: identify Gaussian distribution</h4> <p>The stochastic integral is Gaussian with mean 0. Thus conditional on \(x_0\):</p> <ul> <li>mean: \(\mathbb E[x_t\mid x_0] = \alpha_t x_0.\)</li> <li>covariance: Let \(\varepsilon_t := \alpha_t\int_0^t \alpha_{t'}^{-1} G_{t'} dW_{t'}.\) Then: \(\mathrm{Cov}(\varepsilon_t\mid x_0) = \alpha_t^2 \int_0^t \alpha_{t'}^{-2} G_{t'} \mathrm{Cov}(dW_{t'}) G_{t'}^\top.\) Since \(\mathrm{Cov}(dW_{t'}) = I dt'\): \(\mathrm{Cov}(\varepsilon_t\mid x_0) = \alpha_t^2 \int_0^t \alpha_{t'}^{-2} G_{t'} G_{t'}^\top dt' = \alpha_t^2 \int_0^t \alpha_{t'}^{-2} (2D_{t'}) dt'.\)</li> </ul> <p>Many texts rewrite this as an equivalent expression in terms of the forward-time convention used in the paper: \(S_t := \int_0^t 2D_{t'} \alpha_{t'}^2 dt'.\) (This matches the paper’s definition; it can be obtained by a change of variables depending on whether one defines \(\alpha_t\) relative to 0 or relative to t. The paper uses the above closed form.)</p> <p>Thus: \(p(x\mid x_0,t) = \mathcal N(x; \alpha_t x_0, S_t).\)</p> <hr/> <h3 id="23-derive-the-marginal-at-time-t">2.3 Derive the marginal at time \(t\)</h3> <p>Define the marginal: \(p(x\mid t) := \int p(x\mid x_0,t)\, p_{\text{data}}(x_0)\, dx_0.\)</p> <p><strong>Derivation:</strong> law of total probability / marginalization: \(p(x\mid t) = \int p(x,x_0\mid t)\, dx_0 = \int p(x\mid x_0,t)p_{\text{data}}(x_0)\, dx_0.\)</p> <p>If \(p_{\text{data}}\) is discrete mixture of deltas: \(p(x\mid t) = \frac{1}{M}\sum_{m=1}^M \mathcal N(x; \alpha_t \mu_m, S_t).\) So the forward marginal becomes a Gaussian mixture with components centered at scaled training examples.</p> <h3 id="24-reverse-process-as-an-ode">2.4 Reverse process as an ODE</h3> \[\dot x_t = -\beta_t x_t - D_t\, s(x_t, t),\quad t:T\to \epsilon.\] <p>This is a Probability Flow ODE (PF-ODE), which is equivalent to the reverse SDE for \(p(x_t)\) for all \(t\).</p> <hr/> <h2 id="3-scores-true-score-and-proxy-score-with-full-derivations">3. Scores: true score and proxy score (with full derivations)</h2> <h3 id="31-true-score">3.1 True score</h3> <p>Define the true score: \(s(x,t) := \nabla_x \log p(x\mid t).\)</p> <p>This is the vector field needed by reverse-time sampling methods.</p> <hr/> <h3 id="32-proxy-score-dsm-target-and-its-closed-form">3.2 Proxy score (DSM target) and its closed form</h3> <p>The true score is unknown. To approximate the true score, the proxy score is defined as the score on each data sample, which have a gaussian form: \(\tilde s(x,t;x_0) := \nabla_x \log p(x\mid x_0,t).\) Since: \(p(x\mid x_0,t)=\mathcal N(x; \mu, \Sigma),\quad \mu=\alpha_t x_0,\quad \Sigma=S_t,\) we compute: \(\log \mathcal N(x;\mu,\Sigma) = -\frac{D}{2}\log(2\pi) - \frac{1}{2}\log\det\Sigma - \frac{1}{2}(x-\mu)^\top \Sigma^{-1}(x-\mu).\) Differentiate w.r.t. \(x\): \(\nabla_x \log \mathcal N(x;\mu,\Sigma) = -\frac{1}{2}\nabla_x\left((x-\mu)^\top \Sigma^{-1}(x-\mu)\right).\) Using: \(\nabla_x\left((x-\mu)^\top A (x-\mu)\right)= 2A(x-\mu)\quad \text{for symmetric }A,\) we get: \(\nabla_x \log \mathcal N(x;\mu,\Sigma)= -\Sigma^{-1}(x-\mu) = \Sigma^{-1}(\mu-x).\) Substitute \(\mu\) and \(\Sigma\): \(\tilde s(x,t;x_0) = S_t^{-1}(\alpha_t x_0 - x).\)</p> <hr/> <h2 id="4-compare-objectives-j_0-vs-j_1-and-why-tilde-s-matters">4. Compare objectives \(J_0\) vs \(J_1\) and why \(\tilde s\) matters</h2> <h3 id="41-define-the-objectives">4.1 Define the objectives</h3> <p>Idealized objective: \(J_0(\theta) := \mathbb E_{t,x}\left[\frac{\lambda_t}{2}\|\hat s_\theta(x,t) - s(x,t)\|^2\right].\)</p> <p>DSM objective: \(J_1(\theta) := \mathbb E_{t,x_0,x}\left[\frac{\lambda_t}{2}\|\hat s_\theta(x,t) - \tilde s(x,t;x_0)\|^2\right].\)</p> <p>Here the sampling is:</p> <ul> <li> \[t\sim p(t)\] </li> <li> \[x_0\sim p_{\text{data}}(x_0)\] </li> <li> \[x\sim p(x\mid x_0,t)\] </li> </ul> <hr/> <h3 id="42-unbiasedness-derive-mathbb-e_x_0mid-xttilde-ss">4.2 Unbiasedness: derive \(\mathbb E_{x_0\mid x,t}[\tilde s]=s\)</h3> <p>They prove that the expectation of the proxy score is equal to the true score: \(\mathbb E_{x_0\mid x,t}[\tilde s(x,t;x_0)] = s(x,t).\)</p> <p>Start from definition: \(p(x\mid t)=\int p(x\mid x_0,t)p_{\text{data}}(x_0)\, dx_0.\) Differentiate w.r.t. \(x\): \(\nabla_x p(x\mid t) = \int \nabla_x p(x\mid x_0,t)p_{\text{data}}(x_0)\, dx_0.\) Rewrite: \(\nabla_x p(x\mid x_0,t) = p(x\mid x_0,t)\nabla_x \log p(x\mid x_0,t) = p(x\mid x_0,t)\tilde s(x,t;x_0).\) Thus: \(\nabla_x p(x\mid t)=\int p(x\mid x_0,t)\tilde s(x,t;x_0)p_{\text{data}}(x_0)\, dx_0.\) But: \(p(x_0\mid x,t) = \frac{p(x\mid x_0,t)p_{\text{data}}(x_0)}{p(x\mid t)}.\) So divide by \(p(x\mid t)\): \(\frac{\nabla_x p(x\mid t)}{p(x\mid t)} = \int \tilde s(x,t;x_0) p(x_0\mid x,t)\, dx_0.\) Left-hand side equals: \(\nabla_x \log p(x\mid t) = s(x,t).\) Therefore: \(s(x,t)=\mathbb E_{x_0\mid x,t}[\tilde s(x,t;x_0)].\)</p> <hr/> <h3 id="43-covariance-of-proxy-score">4.3 Covariance of proxy score</h3> <p>Define: \(C_{ij}(x,t) := \mathrm{Cov}_{x_0\mid x,t}[\tilde s_i, \tilde s_j] = \mathbb E[\tilde s_i\tilde s_j] - s_i s_j,\) where expectations are under \(p(x_0\mid x,t)\).</p> <p>We want the identity: \(C(x,t)=S_t^{-1} + \nabla_x^2 \log p(x\mid t).\)</p> <h4 id="step-1-compute-hessian-of-log-marginal">Step 1: compute Hessian of log marginal</h4> <p>Start with: \(s(x,t)=\nabla_x \log p(x\mid t)=\frac{\nabla_x p(x\mid t)}{p(x\mid t)}.\) Differentiate again: \(\nabla_x^2 \log p = \nabla_x\left(\frac{\nabla_x p}{p}\right) = \frac{\nabla_x^2 p}{p} - \frac{(\nabla_x p)(\nabla_x p)^\top}{p^2}.\) So: \(\nabla_x^2 \log p = \frac{\nabla_x^2 p}{p} - s s^\top.\)</p> <h4 id="step-2-express-nabla_x2-pxmid-t-in-terms-of-conditional-objects">Step 2: express \(\nabla_x^2 p(x\mid t)\) in terms of conditional objects</h4> <p>We have: \(p(x\mid t)=\int p(x\mid x_0,t)p_{\text{data}}(x_0)\, dx_0.\) Differentiate twice: \(\nabla_x^2 p(x\mid t)=\int \nabla_x^2 p(x\mid x_0,t)p_{\text{data}}(x_0)\, dx_0.\)</p> <p>Now compute \(\nabla_x^2 p(x\mid x_0,t)\): \(\nabla_x^2 p = \nabla_x(p\tilde s)= (\nabla_x p)\tilde s^\top + p\nabla_x\tilde s = p\tilde s\tilde s^\top + p\nabla_x\tilde s.\) Thus: \(\nabla_x^2 p(x\mid x_0,t) = p(x\mid x_0,t)\left(\tilde s\tilde s^\top + \nabla_x\tilde s\right).\)</p> <p>Integrate: \(\nabla_x^2 p(x\mid t)=\int p(x\mid x_0,t)\left(\tilde s\tilde s^\top + \nabla_x\tilde s\right)p_{\text{data}}(x_0)\, dx_0.\) Divide by \(p(x\mid t)\) to convert to posterior expectation: \(\frac{\nabla_x^2 p(x\mid t)}{p(x\mid t)} = \mathbb E_{x_0\mid x,t}\left[\tilde s\tilde s^\top + \nabla_x\tilde s\right].\)</p> <h4 id="step-3-substitute-into-hessian-identity">Step 3: substitute into Hessian identity</h4> <p>Recall: \(\nabla_x^2 \log p = \frac{\nabla_x^2 p}{p} - s s^\top.\) So: \(\nabla_x^2 \log p = \mathbb E[\tilde s\tilde s^\top + \nabla_x\tilde s] - s s^\top = \mathrm{Cov}(\tilde s) + \mathbb E[\nabla_x\tilde s].\) Therefore: \(\mathrm{Cov}(\tilde s) = \nabla_x^2 \log p - \mathbb E[\nabla_x\tilde s].\)</p> <h4 id="step-4-compute-nabla_xtilde-s-for-gaussian-conditional">Step 4: compute \(\nabla_x\tilde s\) for Gaussian conditional</h4> <p>We have: \(\tilde s(x,t;x_0)=S_t^{-1}(\alpha_t x_0 - x).\) Differentiate w.r.t. \(x\): \(\nabla_x\tilde s = \nabla_x(-S_t^{-1} x) = -S_t^{-1}.\) This is constant (independent of \(x_0\)). So: \(\mathbb E_{x_0\mid x,t}[\nabla_x\tilde s] = -S_t^{-1}.\)</p> <p>Substitute: \(\mathrm{Cov}(\tilde s) = \nabla_x^2 \log p - (-S_t^{-1}) = S_t^{-1} + \nabla_x^2 \log p.\)</p> <p>So: \(C(x,t)=S_t^{-1} + \nabla_x^2 \log p(x\mid t).\)</p> <p><strong>Interpretation (from our discussion):</strong></p> <ul> <li>As \(t\to 0\), \(S_t\to 0\) so \(S_t^{-1}\) blows up: proxy-score covariance large at small times.</li> <li>Large curvature \(\nabla_x^2 \log p\) happens near modes and boundaries (e.g., for discrete mixtures).</li> </ul> <hr/> <h3 id="44-why-proxy-score-covariance-matters-for-training-and-generalization">4.4 Why proxy-score covariance matters for training and generalization</h3> <p>Even though \(\tilde s\) is unbiased for \(s\), training on \(J_1\) uses noisy targets. Empirically, finite neural nets trained on \(J_1\) produce a sampling distribution different from the true-score PF-ODE.</p> <p>The paper’s thesis: <strong>structured noise in the target induces structured variance in the learned estimator</strong>, which translates into stochasticity in typical sampling, yielding generalization.</p> <hr/> <h2 id="5-pf-ode-sampling-and-the-distribution-of-outputs">5. PF-ODE sampling and the “distribution of outputs”</h2> <h3 id="51-pf-ode-dynamics">5.1 PF-ODE dynamics</h3> <p>Sampling uses the probability flow ODE: \(\dot x_t = -\beta_t x_t - D_t s(x_t,t),\quad t:T\to \epsilon.\) In practice, you plug in the learned score estimator \(\hat s_\theta\): \(\dot x_t = -\beta_t x_t - D_t \hat s_\theta(x_t,t).\)</p> <h3 id="52-deterministic-mapping-given-theta">5.2 Deterministic mapping given \(\theta\)</h3> <p>Fix:</p> <ul> <li>the network parameters \(\theta\)</li> <li>the initial noise seed \(x_T\)</li> </ul> <p>Then the ODE solution is deterministic: \(x_0 = F(x_T,\theta).\)</p> <h3 id="53-why-the-output-distribution-uses-a-delta">5.3 Why the output distribution uses a delta</h3> <p>The typical distribution of the generated sample \(\bar q(x_0\mid x_T)\) is defined:</p> \[\bar q(x_0\mid x_T) = \mathbb E_\theta[\delta(x_0 - F(x_T,\theta))]?\] <p>Because this is the standard identity: for any random variable \(Y\), its density can be written as: \(p_Y(y) = \mathbb E[\delta(y-Y)].\)</p> <h4 id="definition-of-dirac-delta">Definition of Dirac delta</h4> <p>The delta is defined by its action under integration: \(\int \delta(x-a) f(x) dx = f(a).\) Thus, if \(Y\) is deterministic with value \(a\), its distribution is \(\delta(x-a)\).</p> <p>Here \(Y=F(x_T,\theta)\) is random only through \(\theta\); averaging those deltas gives the typical distribution.</p> <hr/> <h2 id="6-key-technical-tool-path-integral-representation-of-pf-ode-outputs-eq-6">6. Key technical tool: path integral representation of PF-ODE outputs (Eq. 6)</h2> <h3 id="61-why-we-need-it">6.1 Why we need it</h3> <p>Directly averaging over \(F(x_T,\theta)\) is hard because:</p> <ul> <li>PF-ODE typically has no closed-form solution.</li> <li>\(F\) depends on \(\hat s_\theta\) in a complicated, nonlinear, trajectory-dependent way.</li> </ul> <p>So the paper rewrites the deterministic constraint “this path satisfies the ODE” into an integral form where \(\hat s_\theta\) appears linearly in an exponent.</p> <hr/> <h3 id="62-discrete-time-derivation-of-the-path-integral-idea-most-intuitive">6.2 Discrete-time derivation of the path integral idea (most intuitive)</h3> <p>Discretize time: \(t_k = T - k\Delta t,\quad k=0,1,\dots,N,\quad t_N=\epsilon.\)</p> <p>Euler update for PF-ODE using estimator: \(x_{k+1} = x_k + \Delta t\left(\beta_{t_k} x_k + D_{t_k}\hat s_\theta(x_k,t_k)\right).\) Rearrange: \(x_{k+1} - x_k - \Delta t(\beta_{t_k}x_k + D_{t_k}\hat s_\theta(x_k,t_k)) = 0.\)</p> <p>Enforce each step by a delta: \(\delta\left(x_{k+1}-x_k-\Delta t(\beta_{t_k}x_k + D_{t_k}\hat s_\theta(x_k,t_k))\right).\)</p> <p>Now enforce delta via Fourier representation (finite-dimensional): \(\delta(y) = \int \frac{dp}{(2\pi)^D}\exp(ip\cdot y).\)</p> <p>Introduce an auxiliary \(p_k\) per time step: \(\delta(\cdots) = \int dp_k \exp\left(ip_k\cdot\left[x_{k+1}-x_k-\Delta t(\beta_{t_k}x_k + D_{t_k}\hat s_\theta(x_k,t_k))\right]\right).\)</p> <p>Multiply over all steps and integrate over intermediate states \(\{x_k\}\) and auxiliaries \(\{p_k\}\). In the continuous-time limit, this becomes the functional integral (Eq. 6): \(q(x_0\mid x_T;\theta) = \int \mathcal D[p_t]\mathcal D[x_t]\, \exp\left( \int_T^\epsilon i p_t\cdot[\dot x_t+\beta_t x_t + D_t \hat s_\theta(x_t,t)]dt \right).\)</p> <p>Because integrating over \(p_t\) creates a delta-functional: \(\int \mathcal D[p_t]\exp\left(i\int p_t\cdot R_t[x]dt\right) \propto \delta[R_t[x]].\) This delta-functional equals “1” on paths where \(R_t[x]=0\) (ODE satisfied), and “0” otherwise. So ODE-consistent paths contribute by <strong>surviving</strong>, not by making the exponent nonzero.</p> <hr/> <h2 id="7-ensemble-averaging-cumulant-expansion-to-get-eq-7">7. Ensemble averaging: cumulant expansion to get Eq. 7</h2> <h3 id="71-what-is-random">7.1 What is random?</h3> <p>The score estimator \(\hat s_\theta(x,t)\) is random because \(\theta\) depends on random finite training data (and optimization randomness). So \(q(x_0\mid x_T;\theta)\) is random, and we want its ensemble average: \([q(x_0\mid x_T)] := \mathbb E_\theta[q(x_0\mid x_T;\theta)].\)</p> <h3 id="72-why-the-average-becomes-tractable">7.2 Why the average becomes tractable</h3> <p>Inside Eq. 6, the dependence on \(\hat s_\theta\) is linear in the exponent: \(\exp\left(\int i p_t\cdot D_t \hat s_\theta(x_t,t)dt\right).\) So we need to average an exponential of a linear functional: \(\mathbb E_\theta\left[\exp\left(\int J_t\cdot \hat s_\theta(x_t,t) dt\right)\right],\) where \(J_t := i D_t^\top p_t\).</p> <p>This is a <strong>characteristic functional</strong>. Its log admits a cumulant expansion: \(\log \mathbb E[e^{Z}] = \kappa_1(Z) + \frac{1}{2}\kappa_2(Z) + \frac{1}{6}\kappa_3(Z)+\cdots,\) where \(\kappa_n\) are cumulants.</p> <p>With \(Z\) linear in \(\hat s_\theta\):</p> <ul> <li>\(\kappa_1\) depends on the mean of \(\hat s_\theta\)</li> <li>\(\kappa_2\) depends on the covariance of \(\hat s_\theta\)</li> <li>higher cumulants correspond to non-Gaussianity</li> </ul> <p>Thus the paper obtains: \([q(x_0\mid x_T)] = \int \mathcal D[p_t]\mathcal D[x_t]\, \exp\left(M_1 - \frac{1}{2}M_2 + \cdots\right).\)</p> <h3 id="73-define-s_textavg-and-v-kernel-precisely">7.3 Define \(s_{\text{avg}}\) and V-kernel precisely</h3> <p>Mean score: \(s_{\text{avg}}(x,t) := [\hat s_\theta(x,t)] = \mathbb E_\theta[\hat s_\theta(x,t)].\)</p> <p>Covariance kernel: \(\mathrm{Cov}_\theta[\hat s(x,t),\hat s(x',t')] := \mathbb E_\theta[(\hat s-s_{\text{avg}})(\hat s'-s_{\text{avg}}')^\top].\)</p> <p>V-kernel (as defined by paper): \(V(x,t;x',t') := D_t \mathrm{Cov}_\theta[\hat s(x,t),\hat s(x',t')] D_{t'}.\)</p> <h3 id="74-write-m_1-and-m_2">7.4 Write \(M_1\) and \(M_2\)</h3> <p>\(M_1 := \int_T^\epsilon i p_t\cdot[\dot x_t + \beta_t x_t + D_t s_{\text{avg}}(x_t,t)]dt,\) \(M_2 := \int_T^\epsilon\int_T^\epsilon p_t^\top V(x_t,t;x_{t'},t') p_{t'} dt dt'.\)</p> <hr/> <h2 id="8-gaussian-approximation-and-the-effective-sde-eq-8--proposition-31">8. Gaussian approximation and the Effective SDE (Eq. 8 / Proposition 3.1)</h2> <h3 id="81-dropping-higher-cumulants">8.1 Dropping higher cumulants</h3> <p>Assume the distribution of the estimator across training randomness is approximately Gaussian. Then higher cumulants vanish, so only \(M_1\) and \(M_2\) remain.</p> <h3 id="82-from-quadratic-form-to-noise-the-key-idea">8.2 From quadratic form to noise: the key idea</h3> <p>With a Gaussian field, averaging produces a quadratic term \(M_2\) in the exponent. Quadratic terms in an action correspond to Gaussian noise in an equivalent SDE description.</p> <p>The result: sampling from \([q(x_0\mid x_T)]\) is equivalent to integrating an SDE: \(\dot x_t = -\beta_t x_t - D_t s_{\text{avg}}(x_t,t) + \xi(x_t,t),\) with: \(\mathbb E[\xi(x_t,t)]=0,\) \(\mathbb E[\xi(x_t,t)\xi(x_{t'},t')^\top] = V(x_t,t;x_{t'},t').\)</p> <h3 id="83-key-conceptual-consequence-the-papers-main-interpretive-statement">8.3 Key conceptual consequence (the paper’s main interpretive statement)</h3> <ul> <li>If \(\hat s\) is unbiased so \(s_{\text{avg}}=s\),</li> <li>then deterministic drift matches the true PF-ODE,</li> <li>and the only difference is the noise term controlled by V-kernel.</li> </ul> <p>Thus:</p> <ul> <li>\(V=0\) implies the effective dynamics reduce to deterministic PF-ODE, which (for discrete training data) reproduces training examples (memorization).</li> <li>\(V\ne 0\) implies added stochasticity, causing probability mass to spread and generalize.</li> </ul> <hr/> <h2 id="9-toy-construction-naive-score-estimator-generalizes-proposition-41">9. Toy construction: Naive score estimator generalizes (Proposition 4.1)</h2> <p>This part constructs an explicit scheme showing how a nontrivial V-kernel arises even if one uses proxy scores directly.</p> <h3 id="91-discretized-pf-ode-sampling-with-euler-steps">9.1 Discretized PF-ODE sampling with Euler steps</h3> <p>Integrate backward with Euler: \(x_{t-\Delta t} = x_t + \Delta t\beta_t x_t + \Delta t D_t (\text{score used at time }t).\)</p> <h3 id="92-at-each-step-sample-an-endpoint-x_0t-from-the-posterior">9.2 At each step sample an endpoint \(x_{0t}\) from the posterior</h3> <p>Sample: \(x_{0t} \sim p(x_0\mid x_t,t) = \frac{p(x_t\mid x_0,t)p_{\text{data}}(x_0)}{p(x_t\mid t)}.\) This captures the ambiguity of which training example could have generated the current noisy point.</p> <h3 id="93-define-the-naive-estimator-used-in-that-step">9.3 Define the naive estimator used in that step</h3> <p>Construct: \(\hat s(x_t,t) := s(x_t,t) + \sqrt{\frac{\kappa}{\Delta t}}\left[\tilde s(x_t,t;x_{0t}) - s(x_t,t)\right].\)</p> <p>Interpretation:</p> <ul> <li>The expected estimator equals \(s\) because \(\mathbb E[\tilde s\mid x,t]=s\).</li> <li>But its variance is nonzero because \(\tilde s\) varies with sampled \(x_{0t}\).</li> </ul> <h3 id="94-plug-into-the-euler-update-explicit-form">9.4 Plug into the Euler update (explicit form)</h3> <p>The paper writes: $$ x_{t-\Delta t} = x_t + \Delta t\beta_t x_t + \Delta t D_t s(x_t,t)</p> <ul> <li>\sqrt{\kappa\Delta t}\, D_t\left[\tilde s(x_t,t;x_{0t})-s(x_t,t)\right]. $$</li> </ul> <p>This is exactly of “drift + sqrt(Delta t) noise” form (Euler–Maruyama).</p> <h3 id="95-derive-s_textavgs">9.5 Derive \(s_{\text{avg}}=s\)</h3> <p>Compute ensemble mean over the fresh sampling of \(x_{0t}\): \([\hat s(x_t,t)] = s(x_t,t) + \sqrt{\frac{\kappa}{\Delta t}}\left[\mathbb E(\tilde s\mid x_t,t)-s\right] = s(x_t,t).\)</p> <p>So the mean drift is the true PF-ODE drift.</p> <h3 id="96-derive-the-v-kernel-for-this-scheme">9.6 Derive the V-kernel for this scheme</h3> <p>Noise at time step is: \(\Delta x_{\text{noise}} = \sqrt{\kappa\Delta t}\, D_t(\tilde s - s).\) So its covariance (conditioned on \(x_t,t\)) is: \(\mathrm{Cov}(\Delta x_{\text{noise}}) = \kappa\Delta t\, D_t \mathrm{Cov}(\tilde s\mid x_t,t) D_t.\) But \(\mathrm{Cov}(\tilde s\mid x_t,t)=C(x_t,t)\) where: \(C(x,t)=S_t^{-1} + \nabla_x^2 \log p(x\mid t).\)</p> <p>If the samples \(x_{0t}\) are independent across time steps, then noise is white in time. In continuous limit this yields: \(V(x_t,t;x_{t'},t') = \kappa D_t C(x_t,t) D_{t'} \delta(t-t').\)</p> <h3 id="97-interpret-the-result-why-this-implies-generalization">9.7 Interpret the result (why this implies generalization)</h3> <ul> <li>Even though this scheme uses proxy score draws tied to training data (so “memorization-like”),</li> <li>the resampling injects noise proportional to proxy-score covariance.</li> <li>Proxy-score covariance is high in boundary regions between training examples. So the effective sampling SDE has more diffusion in those boundary regions, which spreads mass between examples: that is generalization.</li> </ul> <hr/> <h2 id="10-final-qualitative-statement-connect-to-your-last-paragraph">10. Final qualitative statement (connect to your last paragraph)</h2> <p>The effective dynamics:</p> <ul> <li>follow deterministic PF-ODE dynamics <strong>in expectation</strong> because noise mean is zero: \(\mathbb E[\xi]=0.\)</li> <li>and are <strong>most likely</strong> to remain near deterministic PF-ODE paths because large deviations have lower probability under Gaussian noise. Thus the model still samples near training examples most of the time, but with nonzero V-kernel it also places mass in-between.</li> </ul> <p>This precisely matches the paper’s intended notion of “generalize but still prioritize training regions.”</p> <hr/> <h2 id="11-consolidated-answers-to-the-specific-questions-you-raised">11. Consolidated answers to the specific questions you raised</h2> <h3 id="111-why-negative-sign-in-forward-sde">11.1 Why negative sign in forward SDE?</h3> <p>Because it is a mean-reverting drift term ensuring stability and convergence to noise. It does not imply \(x_0=-x_0\); it implies \(\dot x_0\) points toward 0.</p> <h3 id="112-why-marginalization-formula-pxmid-tint-pxmid-x_0tp_textdatax_0dx_0">11.2 Why marginalization formula \(p(x\mid t)=\int p(x\mid x_0,t)p_{\text{data}}(x_0)dx_0\)?</h3> <p>It is the law of total probability: marginalize out the latent clean point \(x_0\).</p> <h3 id="113-why-proxy-score-is-s_t-1alpha_t-x_0-x">11.3 Why proxy score is \(S_t^{-1}(\alpha_t x_0-x)\)?</h3> <p>It is the gradient of the log of a Gaussian density in \(x\).</p> <h3 id="114-why-mathbb-etilde-smid-xts">11.4 Why \(\mathbb E[\tilde s\mid x,t]=s\)?</h3> <p>Differentiate the mixture expression for \(p(x\mid t)\) under the integral sign and use Bayes rule.</p> <h3 id="115-why-covariance-formula-cs_t-1nabla_x2log-p">11.5 Why covariance formula \(C=S_t^{-1}+\nabla_x^2\log p\)?</h3> <p>Compute Hessian of log marginal and express it via posterior expectations; use \(\nabla_x \tilde s=-S_t^{-1}\) for Gaussian conditional.</p> <h3 id="116-why-distribution-of-deterministic-output-is-delta">11.6 Why distribution of deterministic output is delta?</h3> <p>For fixed \(\theta,x_T\) the output is exactly \(F(x_T,\theta)\), so: \(q(x_0\mid x_T;\theta)=\delta(x_0-F(x_T,\theta)).\)</p> <h3 id="117-why-ensemble-average-is-mathbb-e_thetadeltax_0-fx_ttheta-and-not-mathbb-ef">11.7 Why ensemble average is \(\mathbb E_\theta[\delta(x_0-F(x_T,\theta))]\) and not \(\mathbb E[F]\)?</h3> <p>Because we want the full distribution (mass placement), not the mean output. The delta identity defines the induced distribution.</p> <h3 id="118-why-path-integral-helps">11.8 Why path integral helps?</h3> <p>It replaces “solve a complicated ODE” with “integrate over paths constrained to satisfy the ODE,” turning dependence on \(\hat s_\theta\) into a linear functional in an exponent so cumulant methods apply.</p> <h3 id="119-why-if-residual-is-zero-how-can-it-contribute">11.9 Why “if residual is zero, how can it contribute”?</h3> <p>Because integrating over \(p_t\) enforces the constraint via a delta-functional; ODE-consistent paths survive with nonzero weight, inconsistent paths cancel out.</p> <h3 id="1110-why-averaging-mathbb-e_thetaexpint-p_tcdot-d_t-hat-s_theta-dt-is-tractable">11.10 Why averaging \(\mathbb E_\theta[\exp(\int p_t\cdot D_t \hat s_\theta dt)]\) is tractable?</h3> <p>Because it is a characteristic functional; its log expands in cumulants determined by mean and covariance of \(\hat s_\theta\).</p> <hr/> <h2 id="12-generalization-through-variance-consequences-and-example">12 Generalization Through Variance: Consequences and Example</h2> <p>This section discusses the conceptual and practical consequences of generalization through variance.<br/> Rather than introducing new technical machinery, it explains what kind of inductive bias the theory implies, when generalization occurs or does not occur, and how the V-kernel shapes the learned distribution.</p> <hr/> <h3 id="121-benign-properties-of-generalization-through-variance">12.1 Benign Properties of Generalization Through Variance</h3> <p>The theory developed earlier shows that generalization arises through the V-kernel, which is determined by the covariance of the proxy score. Crucially, this covariance has a highly structured form, which strongly constrains how generalization can occur.</p> <hr/> <h4 id="1211-boundary-driven-generalization">12.1.1 Boundary-driven generalization</h4> <p>The key driver of generalization through variance is the proxy score covariance, which is large primarily in boundary regions between training examples. These are regions where the posterior distribution</p> \[p(x_0 \mid x,t)\] <p>is uncertain, meaning that multiple training points could plausibly explain the same noisy observation $x_t$.</p> <p>As a result, generalization through variance:</p> <ul> <li>acts mainly in ambiguous regions between data points,</li> <li>does not create probability mass arbitrarily far from the data,</li> <li>and is therefore strongly constrained.</li> </ul> <p>This boundary-localized behavior is why generalization through variance provides a reasonable inductive bias.</p> <hr/> <h4 id="1212-no-generalization-for-a-single-data-point">12.1.2 No generalization for a single data point</h4> <p>If the dataset contains only one training example (so that $M = 1$), then the posterior $p(x_0 \mid x,t)$ is deterministic. In this case, the proxy score covariance vanishes:</p> \[\mathrm{Cov}_{x_0 \mid x,t}[\tilde s(x,t;x_0)] = 0.\] <p>Since the V-kernel is constructed from this covariance, it follows that</p> \[V(x,t;x',t') = 0.\] <p>Therefore:</p> <ul> <li>the effective dynamics reduce exactly to the deterministic PF-ODE,</li> <li>no stochasticity is introduced,</li> <li>and no generalization occurs.</li> </ul> <p>This shows that generalization requires ambiguity in the data distribution.</p> <hr/> <h4 id="1213-preservation-of-data-manifold-dimensionality">12.1.3 Preservation of data manifold dimensionality</h4> <p>If the data distribution is supported primarily on a low-dimensional data manifold, then uncertainty in $p(x_0 \mid x,t)$ tends to lie along the manifold, not orthogonal to it.</p> <p>Consequently:</p> <ul> <li>the proxy score covariance is nontrivial only along manifold directions,</li> <li>the V-kernel injects noise only in those directions.</li> </ul> <p>Thus, generalization through variance:</p> <ul> <li>preserves the intrinsic dimensionality of the data,</li> <li>rather than spreading probability mass into irrelevant regions of the ambient space.</li> </ul> <hr/> <h4 id="1214-no-extrapolation-far-from-training-data">12.1.4 No extrapolation far from training data</h4> <p>Very far from training examples, the posterior $p(x_0 \mid x,t)$ becomes sharply peaked. In this regime, the proxy score covariance is approximately zero, and therefore the V-kernel vanishes.</p> <p>As a result:</p> <ul> <li>the effective dynamics become deterministic,</li> <li>and no generalization through variance occurs.</li> </ul> <p>This explains why diffusion models do not extrapolate aggressively far beyond the support of the training data.</p> <hr/> <h4 id="1215-why-deterministic-structure-is-largely-preserved">12.1.5 Why deterministic structure is largely preserved</h4> <p>The effective dynamics derived earlier take the form of a stochastic differential equation:</p> \[\dot x_t = -\beta_t x_t - D_t s_{\mathrm{avg}}(x_t,t) + \xi(x_t,t),\] <p>where the noise term satisfies</p> \[\mathbb E[\xi(x_t,t)] = 0.\] <p>Because the noise has zero mean:</p> <ul> <li>the effective PF-ODE follows deterministic PF-ODE dynamics on average.</li> </ul> <p>Moreover, paths that deviate significantly from deterministic PF-ODE trajectories are less likely, since such deviations incur a higher noise cost. Therefore:</p> <ul> <li>the most probable trajectories remain close to deterministic ones.</li> </ul> <p>Although the effective dynamics differ from the deterministic PF-ODE, they do not differ substantially. Regions near training data, which are attractors of the deterministic flow, will still tend to be sampled most frequently.</p> <hr/> <h3 id="122-memorization-and-the-v-kernel-in-the-small-noise-limit">12.2 Memorization and the V-kernel in the Small-Noise Limit</h3> <p>While the effective SDE description explains generalization qualitatively, it does not immediately reveal how the V-kernel reshapes the learned distribution.</p> <p>To gain further insight, the paper considers a small-noise approximation, which is valid when the model is somewhat underparameterized. This corresponds to the regime where</p> \[\kappa = \frac{F}{P} &lt; 1,\] <p>so the V-kernel-induced noise is weak.</p> <hr/> <h4 id="1221-semiclassical-approximation">12.2.1 Semiclassical approximation</h4> <p>When the noise is sufficiently small, the path integral describing the typical learned distribution can be approximated using a semiclassical (saddle-point) approximation.</p> <p>In this limit, the average learned distribution satisfies</p> \[[q(x_0)] \approx p(x_0 \mid \epsilon) \, \frac{1}{\sqrt{ \det\!\left( \frac{1}{\kappa} \frac{\partial^2 S_{\mathrm{cl}}(x_0, x_T^\ast(x_0))} {\partial x_T \partial x_T} \right) }}.\] <p>Here:</p> <ul> <li>$p(x_0 \mid \epsilon)$ is the data distribution convolved with a small amount of diffusion noise,</li> <li>$x_T^\ast(x_0)$ is the most likely noise seed that flows to $x_0$ under deterministic PF-ODE dynamics,</li> <li>$S_{\mathrm{cl}}$ is the action (negative log-likelihood) of the most likely path connecting $x_T^\ast$ to $x_0$.</li> </ul> <hr/> <h4 id="1222-interpretation-of-the-small-noise-limit">12.2.2 Interpretation of the small-noise limit</h4> <p>In words:</p> <ul> <li>the typical learned distribution equals the slightly noised data distribution,</li> <li>multiplied by a curvature factor that measures how sensitive PF-ODE trajectories are to small perturbations.</li> </ul> <p>The V-kernel affects generalization only through this curvature term, by changing the likelihood of small deviations from deterministic PF-ODE dynamics.</p> <p>Although this approximation clarifies the mechanism, the paper notes that it remains difficult to obtain more explicit analytic results beyond this limit.</p> <hr/> <h3 id="123-gap-filling-inductive-bias">12.3 Gap-Filling Inductive Bias</h3> <p>Because the V-kernel is largest in boundary regions between training examples, generalization through variance is often expected to fill in gaps between data points.</p> <p>However, this behavior is not guaranteed in all cases.</p> <hr/> <h4 id="1231-naive-behavior-reduced-boundary-probability">12.3.1 Naive behavior: reduced boundary probability</h4> <p>In naive settings, generalization through variance can actually reduce the probability mass associated with boundary regions. This occurs because increased noise in those regions can cause trajectories to pass through them more quickly.</p> <hr/> <h4 id="1232-role-of-temporal-correlations">12.3.2 Role of temporal correlations</h4> <p>If the model exhibits nontrivial temporal generalization, for example through time-dependent features $\phi(x,t)$, the V-kernel may develop nontrivial temporal autocorrelations.</p> <p>The paper speculates that such temporal structure may allow trajectories to spend more time in boundary regions, enabling gap-filling behavior.</p> <hr/> <h3 id="124-dependence-on-epsilon-and-model-capacity">12.4 Dependence on $\epsilon$ and Model Capacity</h3> <p>The details of generalization depend strongly on two parameters:</p> <ol> <li>The time cutoff $\epsilon$,</li> <li>The ratio $\frac{F}{P}$, which controls whether the model is under- or overparameterized.</li> </ol> <p>Larger values of either parameter increase the magnitude of the V-kernel, leading to larger deviations from deterministic PF-ODE dynamics.</p> <p>In illustrative one-dimensional examples with training points ${-1,0,1}$, the average learned distribution differs from both the true distribution and the deterministic PF-ODE approximation in:</p> <ul> <li>the heights of peaks near training data,</li> <li>and the probability mass between them.</li> </ul> <p>These differences increase as $\epsilon$ and $\frac{F}{P}$ grow, but larger deviation does not necessarily imply better generalization.</p> <hr/> <h3 id="125-featurenoise-alignment">12.5 Feature–Noise Alignment</h3> <p>Finally, the paper emphasizes that generalization depends strongly on the interaction between model features and the proxy score covariance.</p> <p>Different feature sets interact with the V-kernel differently, producing qualitatively different generalization behavior.</p> <p>Experiments show that:</p> <ul> <li>the same data distribution can be generalized in different ways,</li> <li>depending on the orientation of the data,</li> <li>and depending on the feature set used (e.g., Gaussian versus Fourier features).</li> </ul> <p>Thus, generalization through variance is not universal: it is shaped by feature–noise alignment, which determines which gaps are filled and which are not.</p>]]></content><author><name></name></author><category term="Paper-Reading"/><category term="Diffusion"/><category term="Generalization"/><summary type="html"><![CDATA[Paper summary for "Generalization through Variance in Diffusion Models"]]></summary></entry><entry><title type="html">How Diffusion Models Work</title><link href="https://jefferyy-peng.github.io/blog/2025/diffusion-model/" rel="alternate" type="text/html" title="How Diffusion Models Work"/><published>2025-03-19T16:40:16+00:00</published><updated>2025-03-19T16:40:16+00:00</updated><id>https://jefferyy-peng.github.io/blog/2025/diffusion-model</id><content type="html" xml:base="https://jefferyy-peng.github.io/blog/2025/diffusion-model/"><![CDATA[<p>As part of my research into the mechanisms of compositionality in generative AI, I am conducting a review of the foundational literature surrounding Diffusion Models. While these models have achieved ubiquity in text-to-image generation, understanding their underlying mathematical formulations and architectural constraints is essential for addressing their current limitations in factorization and generalization.</p> <p>This post summarizes the theoretical framework of diffusion models, covering the probabilistic definitions, conditioning mechanisms, and the trade-offs between pixel-space and latent-space architectures.</p> <hr/> <h2 id="21-formulations-of-diffusion-models">2.1 Formulations of Diffusion Models</h2> <p>Diffusion models can be formalized through three primary frameworks: <strong>Denoising Diffusion Probabilistic Models (DDPMs)</strong>, <strong>Score-based Generative Models (SGMs)</strong>, and <strong>Stochastic Differential Equations (SDEs)</strong>.</p> <h3 id="211-denoising-diffusion-probabilistic-models-ddpms">2.1.1 Denoising Diffusion Probabilistic Models (DDPMs)</h3> <p>The DDPM framework defines a forward Markov chain that gradually adds Gaussian noise to data \(x_0\) until it approaches an isotropic Gaussian distribution \(x_T\). The transition kernel is defined as: \(q(x_t \mid x_{t-1})=\mathcal{N}\!\left(x_t;\sqrt{1-\beta_t}\,x_{t-1},\beta_t I\right),\) where \(\beta_t \in (0,1)\) controls the noise variance.</p> <p>Let \(\alpha_t := 1-\beta_t\) and \(\bar{\alpha}_t := \prod_{s=1}^{t} \alpha_s\). Then the marginal distribution is:</p> \[q(x_t \mid x_0)=\mathcal{N}\!\left(x_t;\sqrt{\bar{\alpha}_t}\,x_0,(1-\bar{\alpha}_t)I\right).\] <p>Using the reparameterization trick with \(\epsilon \sim \mathcal{N}(0,I)\), we can write:</p> \[x_t=\sqrt{\bar{\alpha}_t}\,x_0 + \sqrt{1-\bar{\alpha}_t}\,\epsilon.\] <p>The generative capability arises from learning a parameterized reverse Markov chain. The model estimates the transition kernel \(p_\theta(x_{t-1}\mid x_t)\) to iteratively denoise the latent variables:</p> \[p_\theta(x_{t-1} | x_t) = \mathcal{N}(x_{t-1}; \mu_\theta(x_t, t), \Sigma_\theta(x_t, t))\] <p>Training is typically performed by optimizing a variational lower bound (ELBO) on the negative log-likelihood.</p> <blockquote> <p><strong>Step 1</strong>: Marginalize likelihood: the probability of \(x_0\) is obtained by integrating out all latent variables \(x_{1:T}\). This integral is generally intractable.</p> \[\mathbb{E}\big[-\log p_\theta(x_0)\big] = \mathbb{E}\left[-\log \int p_\theta(x_{0:T}) \, dx_{1:T}\right]\] <p><strong>Step 2</strong>: Introduce an auxiliary (variational) distribution \(q(x_{1:T}\mid x_0)\) by multiplying and dividing inside the integral.</p> \[= \mathbb{E}\left[ -\log \int p_\theta(x_{0:T}) \frac{q(x_{0:T})}{q(x_{0:T})} \, dx_{1:T} \right]\] <p><strong>Step 3</strong>: Apply Jensen’s inequality \(-\log\) is convex, moving the log inside the expectation. This produces an upper bound on the negative log-likelihood.</p> \[\le \mathbb{E}_q\left[ -\log \frac{p_\theta(x_{0:T})}{q(x_{1:T}\mid x_0)} \right]\] <p><strong>Step 4</strong>: Next, factorize the model and variational distributions</p> \[p_\theta(x_{0:T}) = p(x_T)\prod_{t=1}^{T} p_\theta(x_{t-1}\mid x_t), \quad q(x_{1:T}\mid x_0) = \prod_{t=1}^{T} q(x_t\mid x_{t-1})\] <p><strong>Step 5</strong>: Substituting these factorizations:</p> \[= \mathbb{E}_q\left[ -\log p(x_T)- \sum_{t=1}^{T} \log \frac{p_\theta(x_{t-1}\mid x_t)} {q(x_t\mid x_{t-1})} \right] := \mathcal{L}\] <p>The first term \(\mathbb{E}_q[-\log p(x_T)]\) is fixed and not optimizable, so we optimize the second term:</p> \[\mathbb{E}_{q(x_{t-1}, x_t)} \left[ \log \frac{q(x_t \mid x_{t-1})} {p_\theta(x_{t-1} \mid x_t)} \right] = \mathbb{E}_{q(x_0, x_t)} \Big[ \mathrm{KL}\big( q(x_{t-1} \mid x_t, x_0) \;\|\; p_\theta(x_{t-1} \mid x_t) \big) \Big]+\text{const}\] <p>this is equavalent to a noise-prediction objective, where a neural network \(\epsilon_\theta\) (typically a UNet) minimizes the error between the added noise \(\epsilon\) and the predicted noise:</p> \[L = \mathbb{E} [ \lambda(t)||\epsilon - \epsilon_\theta(x_t, t)||^2 ]\] </blockquote> <h3 id="212-score-based-generative-models-sgms-and-sdes">2.1.2 Score-based Generative Models (SGMs) and SDEs</h3> <p>Alternative formulations focus on the gradient of the log-density of the data instead of noise being added, known as the score function \(\nabla_x \log p(x)\).</p> <ul> <li><strong>SGMs:</strong> In score-based generative modeling, we train a <strong>Noise-Conditioned Score Network (NCSN)</strong> \(s_\theta(x,t)\) to approximate the score (gradient of the log-density) of data corrupted by Gaussian noise. The classical score-matching objective is</li> </ul> \[\frac{1}{2}\,\mathbb{E}_{p_{\text{data}}} \big\| s_\theta(x) - \nabla_x \log p_{\text{data}}(x) \big\|^2 .\] <p>Let \(x_0 \sim q(x_0)\) denote clean data, and define a family of noisy distributions via</p> \[q(x_t \mid x_0) = \mathcal{N}(x_t; x_0, \sigma_t^2 I), \qquad q(x_t) = \int q(x_t \mid x_0) q(x_0)\,dx_0 .\] <p>Conditioning the score network on the noise level \(t\), the score-matching objective becomes (up to a constant factor):</p> \[\mathbb{E}_{t \sim \mathcal{U}[1,T],\, x_0 \sim q(x_0),\, x_t \sim q(x_t \mid x_0)} \left[\lambda(t)\, \big\| s_\theta(x_t, t) - \nabla_x \log q(x_t \mid x_0) \big\|^2 \right].\] <p>The relation between DDPM and SGM is then</p> \[\boxed{ \epsilon_\theta(x,t) = -\,\sigma_t\, s_\theta(x,t) }\] <ul> <li><strong>SDEs:</strong> While DDPMs and SGMs are originally defined using a <strong>finite, discretized noising process</strong>, the diffusion process can be equivalently formulated in <strong>continuous time</strong> as a stochastic differential equation (SDE). This continuous view unifies diffusion models and enables flexible sampling algorithms.</li> </ul> <h4 id="general-forward-diffusion-sde">General Forward Diffusion SDE</h4> <p>The forward noising process is described by the <strong>Score SDE</strong>:</p> \[dx = f(x,t)\,dt + g(t)\,dw,\] <p>where:</p> <ul> <li>\(f(x,t)\) is the <strong>drift (diffusion) term</strong>,</li> <li>\(g(t)\) controls the <strong>noise magnitude</strong>,</li> <li>\(w\) is a standard Wiener process.</li> </ul> <p>This SDE defines a family of marginal distributions \(q_t(x)\) over time.</p> <h4 id="ddpm-as-a-special-case">DDPM as a Special Case</h4> <p>The continuous-time limit of DDPM corresponds to the SDE:</p> \[dx = -\frac{1}{2}\beta(t)\,x\,dt + \sqrt{\beta(t)}\,dw,\] <p>where \(\beta(t)\) is a continuous noise schedule. This formulation mirrors the discrete DDPM noising process in the limit of infinitely many steps.</p> <h4 id="sgm-as-a-special-case">SGM as a Special Case</h4> <p>Score-based Generative Models (SGMs) correspond to the SDE:</p> \[dx = \sqrt{\frac{d[\sigma_t^2]}{dt}}\,dw,\] <p>where \(\sigma(t)\) is the continuous noise scale. Here, the forward process is a pure diffusion without drift.</p> <h4 id="reverse-time-sde-sampling-process">Reverse-Time SDE (Sampling Process)</h4> <p>For any forward SDE of the form above, the <strong>reverse-time SDE</strong> is given by:</p> \[dx = \big[f(x,t) - g(t)^2 \nabla_x \log q_t(x)\big]\,dt + g(t)\,d\bar{w},\] <p>where:</p> <ul> <li>\(\nabla_x \log q_t(x)\) is the <strong>score function</strong>,</li> <li>\(\bar{w}\) is a backward-time Wiener process.</li> </ul> <p>Learning the score function enables generation of samples by simulating this reverse SDE.</p> <h4 id="probability-flow-ode">Probability Flow ODE</h4> <p>In addition to the reverse SDE, there exists an equivalent <strong>deterministic ODE</strong> with identical marginal distributions:</p> \[dx = \left[f(x,t) - \frac{1}{2}g(t)^2 \nabla_x \log q_t(x)\right]\,dt.\] <p>This <strong>probability flow ODE</strong> allows sampling without stochasticity, using standard ODE solvers.</p> <h4 id="sampling-methods">Sampling Methods</h4> <p>Given the learned score function, samples can be generated using:</p> <ul> <li>Reverse-time SDE solvers,</li> <li>Probability flow ODE solvers,</li> <li>Annealed Langevin Dynamics,</li> <li>Predictor–Corrector (PC) samplers combining SDE solvers with MCMC methods (e.g., Langevin MCMC or HMC).</li> </ul> <h2 id="22-conditional-generation-mechanisms">2.2 Conditional Generation Mechanisms</h2> <table> <tbody> <tr> <td>To enable controllable generation (e.g., text-to-image synthesis), diffusion models must incorporate a conditional vector $c$. The reverse process is modified to $p_\theta(x_{t-1}</td> <td>x_t, c)$. Two primary guidance methods dominate the literature:</td> </tr> </tbody> </table> <h3 id="221-classifier-guidance">2.2.1 Classifier Guidance</h3> <p>This approach leverages an auxiliary classifier $p_\phi(c | x_t)$ trained on noisy images. The denoising process can be expressed: \(p_{\theta,\phi}(x_t \mid x_{t+1}, c)=Z \, p_\theta(x_t \mid x_{t+1}) \, p_\phi(c \mid x_t)\) where \(Z\) is a normalization constant. Taking the gradient of the log of the conditional density (ignoring \(Z\)):</p> \[\nabla_{x_t} \log \big( p_\theta(x_t \mid x_{t+1}) \, p_\phi(c \mid x_t) \big)=\nabla_{x_t} \log p_\theta(x_t \mid x_{t+1}) + \nabla_{x_t} \log p_\phi(c \mid x_t).\] <p>Using the relationship between score and noise prediction,</p> \[\epsilon_\theta(x_t, t) = -\sigma_t s_\theta(x_t, t),\] <p>the gradient becomes:</p> <p>\(=-\frac{1}{\sigma_t}\,\epsilon_\theta(x_t, t)+\nabla_{x_t} \log p_\phi(c \mid x_t).\)</p> <blockquote> <p><strong>Key Intuition:</strong> beside the original unconditional score function, the gradient of the classifier is added to guide the denoise process.</p> </blockquote> <h3 id="222-classifier-free-guidance">2.2.2 Classifier-free Guidance</h3> <p>To avoid the computational cost and complexity of training a separate noise-robust classifier, Ho and Salimans (2022) proposed classifier-free guidance. Here, a single diffusion model is trained to handle both conditional and unconditional inputs (where $c = \emptyset$). During sampling, the noise prediction is a linear combination of both outputs, weighted by a scale $w$:</p> \[\tilde{\epsilon}_\theta(x_t, c) = (1 + w)\epsilon_\theta(x_t, c) - w\epsilon_\theta(x_t)\] <p>This method implicitly maximizes the probability of the condition without an external classifier and has become the standard for state-of-the-art models.</p> <blockquote> <p><strong>Key Intuition:</strong> trains one diffusion model to operate in two modes: with condition and without condition. During sampling, the model compares these two predictions. The difference tells you how the condition should push the sample, and scaling that difference lets you control how strongly the generation follows the condition. In effect, the model learns its own “internal classifier gradient” and uses it to guide sampling—without ever training an explicit classifier.</p> </blockquote> <h2 id="23-state-of-the-art-architectures">2.3 State-of-the-Art Architectures</h2> <p>Current text-to-image systems are generally categorized by whether the diffusion process occurs in pixel space or latent space.</p> <h3 id="231-pixel-based-models">2.3.1 Pixel-based Models</h3> <p>These models operate directly on high-dimensional image data.</p> <ul> <li><strong>GLIDE:</strong> Uses classifier-free guidance to generate photorealistic images and demonstrates capabilities in text-guided inpainting [15].</li> <li><strong>Imagen:</strong> A key finding from the development of Imagen is the scaling law regarding text encoders. The authors discovered that increasing the size of the language model (e.g., using T5-XXL) yields greater improvements in image fidelity and image-text alignment than increasing the size of the visual diffusion model itself [16].</li> </ul> <h3 id="232-latent-based-models">2.3.2 Latent-based Models</h3> <p>To address the high computational costs of pixel-space diffusion, <strong>Latent Diffusion Models (LDMs)</strong> utilize an autoencoder to project data into a lower-dimensional latent space [18].</p> <ul> <li><strong>Stable Diffusion:</strong> Applies the diffusion process within this compressed latent space, utilizing cross-attention mechanisms to incorporate text conditioning. This architecture significantly improves inference efficiency [19].</li> <li><strong>DALL-E 2 (unCLIP):</strong> Utilizes the CLIP embedding space. It generates an image embedding from text and then decodes this embedding into an image, leveraging the joint multimodal space learned by CLIP [20].</li> </ul> <h2 id="24-failure-modes-and-limitations">2.4 Failure Modes and Limitations</h2> <p>Despite the fidelity of these models, systematic evaluations reveal persistent limitations in their reasoning capabilities:</p> <ol> <li><strong>Attribute Binding:</strong> Models frequently fail to correctly bind attributes to objects. For example, in a prompt specifying a “red cube” and a “blue cube,” DALL-E 2 may swap the colors or textures [22], [23].</li> <li><strong>Text Rendering:</strong> While semantic understanding is high, the ability to render coherent alphanumeric text remains poor, likely due to tokenization schemes (BPE encoding) that obscure spelling information from the model [23], [24].</li> <li><strong>Physical Consistency:</strong> Generated images often exhibit violations of physical laws, such as incorrect shadow placement or reflections that do not align with the object’s geometry [25].</li> <li><strong>Bias toward Canonical Forms:</strong> When prompted with unusual scenarios (e.g., “a car with triangular wheels”), models often revert to the mean of the training distribution (circular wheels), indicating a lack of true compositional generalization [26].</li> </ol> <p>These failure modes suggest that while diffusion models excel at texture synthesis and semantic association, they struggle with precise factorization and compositional reasoning—a core focus of the subsequent chapters of this thesis [27].</p>]]></content><author><name></name></author><category term="Learning"/><category term="Diffusion"/><summary type="html"><![CDATA[A learning notebook for diffusion models]]></summary></entry></feed>